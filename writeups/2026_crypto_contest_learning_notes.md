# 2026 암호분석경진대회 문제 분석 노트

이 문서는 `cryptotest/problems`에 들어 있는 8개 문제를 학습용으로 정리한 것이다. 문제별 상세 페이지는 공통적으로 `문제`, `배경지식`, `풀이`, `검증`, `참고 자료` 구조를 따른다.

## 전체 요약

| 번호 | 주제 | 현재 상태 | 상세 페이지 |
| --- | --- | --- | --- |
| 1 | 고전 암호 | 풀이 완료 | [01_암호분석](01_암호분석.md) |
| 2 | permutation 구현 | 풀이 완료 | [02_암호구현](02_암호구현.md) |
| 3 | TLS GCM 위조 | 풀이 완료 | [03_네트워크보안](03_네트워크보안.md) |
| 4 | LLM steganography forensics | 자료 부족, 방법론 정리 | [04_디지털포렌식](04_디지털포렌식.md) |
| 5 | textbook-BGV | 풀이 완료 | [05_동형암호](05_동형암호.md) |
| 6 | Dual_EC_DRBG | 풀이 완료 | [06_PRNG](06_PRNG.md) |
| 7 | RSA partial key exposure | 부분 분석, T=768 mixed-tail과 dynamic T sweep probe 진행 중 | [07_소인수분해](07_소인수분해.md) |
| 8 | 변형 AES 7라운드 | 풀이 완료 | [08_블록암호](08_블록암호.md) |

## 재현 스크립트

완전 풀이 또는 부분 분석을 재현하는 파이썬 스크립트는 `solutions/` 아래에 있다.

- `solve_01_classical.py`: 1번 IC, Caesar, Vigenere 키 분석
- `solve_02_permutation.py`: 2번 permutation 순서와 회전량 복원
- `solve_03_tls_gcm_nonce_reuse.py`: 3번 TLS AES-GCM nonce reuse 위조
- `solve_05_bgv.py`: 5번 BGV secret, 날짜, State 복원
- `solve_06_prng.py`: 6번 Dual_EC 백도어 스칼라와 다음 출력 예측
- `solve_08_aes_key.py`: 8번 변형 AES master key 복원과 50,000개 pair 검증
- `investigate_05_bgv.py`: 5번 BGV 인스턴스 파싱과 차분식 확인
- `investigate_07_rsa_partial_bits.py`: 7번 RSA mask, 미지 bit 구간, `q mod 2^150` 확인
- `try_07_sage_coppersmith.py`: 7번 Sage/crypto-attacks Coppersmith, folded p/q, coarsened 변수 모델 실험
- `solve_07_cuso.py`: 7번 cuso optimized-shift, branch mode, graph-slack, 실제 shift enumeration weight-bias, shift-window, capped shift/multiplicity probe
- `solve_07_hybrid_coron.py`: 7번 `x6` 상위 bit branch + folded bivariate Coron raw/primitive margin 진단, q-fixed range/value sweep, projected-HNF 대표 branch 실험
- `solve_07_jm_3var.py`: 7번 3변수 exact Jochemsz-May integer-root 모델 대표 branch 실험
- `solve_07_jm_lowlift.py`: 7번 `q[210..264]`를 `x1` affine 함수로 제거한 low-lifted Jochemsz-May 모델
- `solve_07_jm_liftT.py`: 7번 `T=265/362/440/600/608/784` symbolic 2-adic lift와 `T=600/608/784` tail-variable 축소 JM 모델
- `sweep_07_liftT_branches.py`: 7번 `x6` top-edge prefix별 `G_T` bound/weighted-height sweep
- `try_07_hensel_prefix_cp_sat.py`: 7번 `T=924` Hensel prefix-only 12비트 limb CP-SAT cube filter prototype
- `try_07_hensel_tail_cp_sat.py`: 7번 `T=768/784/800/816/832/848/1024` Hensel exact/mixed-tail 16비트 limb CP-SAT prototype, final carry zero, carry interval tightening, q interval lex bound, `--lowlift-q 265/272`, `--compact-q-limbs`, odd-residue filters, product-domain narrowing, randomized CP-SAT seed options, JSON summary output
- `try_07_z3_tail_bv.py`: 7번 Hensel tail-lock 조건을 Z3 bit-vector 곱셈으로 검증하는 대체 solver prototype
- `investigate_07_carry_limb.py`: 7번 16비트/4비트 limb 양방향 carry propagation, frontier value emission, `x6_top`/fixed p range branch, exact carry-set, branch pruning 강도 측정
- `try_07_cp_sat_limb.py`: 7번 16비트 limb 곱셈 CP-SAT 모델, fixed p range, edge decision strategy, low-lift q middle affine constraint
- `cube_07_edge_driver.py`: 7번 `x1/x6` edge cube ranking, `x1` high-prefix cube, dynamic low-boundary carry 지표, `tail848` ranking, fast-tail-only broad scan, JSONL/shard/tail-plan export, `x0/x7` branch sweep, 상위 cube CP-SAT/T=848 exact-tail probe, tail probe용 `--lowlift-q`, `--compact-q-limbs`, odd-residue, q-interval, 기본 search, randomized seed sweep, dynamic `--tail-T-values`, small-tail unknown enumeration, extra p/q fixed range plan 옵션 전달
- `run_07_tail_plan.py`: 7번 tail-plan JSONL을 병렬 실행하고 각 CP-SAT probe의 JSON summary를 결과 JSONL로 수집하는 runner
- `run_07_go_sat_filter.py`: 7번 Go DIMACS exporter와 PySAT CaDiCaL을 연결해 full `x1/x2low7/x6` shortlist cube를 SAT/UNSAT로 필터링하는 runner. `--assume-x2low7`로 `x2low7`을 CNF 재생성 없이 PySAT assumptions로 sweep할 수 있고, `--x1 ... --x2low7-all`로 전체 128개 `x2low7` cases를 자동 생성한다. `--assume-x1high7-x2low7`은 `x1low32/x6`만 고정한 CNF에서 `x1high7`과 `x2low7`을 assumptions로 걸어 base 하나당 `128*128` 조합을 훑는다. `--assume-x6low-x1high7-x2low7`은 `x6high/x1low32` base CNF에서 `x6low`, `x1high7`, `x2low7`을 같이 assumptions로 훑고, `--summary-json/--summary-only`로 대량 sweep 결과를 base별 요약 JSON으로 남긴다.
- `compare_07_tail_oracle.py`: 7번 Go/PySAT `tail_window` weak filter와 더 큰 product-prefix oracle(`arith_bits=T+window`)을 같은 assumption tuple에서 비교하는 guardrail runner. exact-tail carry-vector CNF를 만들 때 false UNSAT 여부를 확인하기 위한 회귀 테스트로 쓴다.
- `go_hensel_tail/`: 7번 Hensel-tail 모델을 Go/CNF exporter로 옮기기 위한 scaffold. 현재 JSON cube 입력, `T=784/800/848` 16비트 limb column metadata, DIMACS fixed-bit clause 출력, `p/q` 변수 map JSON export, product-prefix arithmetic CNF, known-prefix carry seed를 구현했다. product-prefix CNF는 2048비트 product 폭까지 확장되어 `arith_bits=1040` shortlist smoke도 가능하고, 상수 입력 adder simplification으로 대표 1040비트 CNF가 `777622` vars / `3453804` clauses까지 줄었다. optimized tail-column equality와 solver model decode는 아직 미구현이다.
- `sanity_07_mask_variants.py`: 7번 mask bit/byte reverse 해석 sanity check
- `diagnose_07_edge_folded.py`: 7번 `x1/x6` edge run 고정 가정에서 folded Coron primitive margin 진단
- `investigate_08_aes_leak.py`: 8번 AES leak pair, 직접 key byte, 후보 집합 확인

## 검증 상태

다음 항목은 스크립트 실행으로 재검증했다.

- 1번: `ciphertexts1`의 IC `0.067638`, Caesar shift `6`, `ciphertexts2`의 Vigenere key `KLVOJ` 확인
- 2번: 모든 1-round testvector 통과, 20-round testvector 통과
- 3번: nonce reuse, GHASH key `112332a84132bc0c5c23a61037723683`, forged TLS record assertion 통과
- 5번: `s` 복구, `20260410 -> 20260411`, `State = BGV DAILY STATUS CORE-A LINK OK TEMP NORMAL POWER STABLE` 확인
- 6번: `P == d*Q`, 상태 후보 검증, `r3=0x2443c8daf1a9d52b09` 확인
- 7번: mask 파싱, unknown interval, `q mod 2^150`, nibble별 `q` MSB prefix, SAT/SMT, folded p/q, coarsened Coppersmith, cuso strict-bound/optimized-shift/graph-slack/weighted-shift/shift-window/one-sided branch probe, hybrid folded Coron raw/primitive margin 및 q-fixed 8/16/24비트 sweep/projected-HNF 대표 branch, 3변수 JM sweep, low-lifted JM, symbolic `T=600/608/784` lift, 16비트/4비트 양방향 carry와 frontier emission/exact carry-set/CP-SAT 모델 확인. folded Coron raw margin은 content `2^210` 때문에 과대평가였고 primitive margin은 `s=9`에서 약 `-132`, q-fixed 24비트에서도 약 `-108`이다. `x1/x6` edge run이 고정되면 primitive margin이 약 `+10`이므로 다음 목표는 이 두 run 회수다. fixed p range와 CP-SAT edge decision strategy, dynamic `q mod 2^t`, composite cube driver, low-lift `q[210..264] = Q0 + C1*x1` 모델을 추가했다. `T=600` symbolic lift는 q tail `Y`를 567비트에서 232비트 수준으로 줄였고, `T=608`은 `Y`를 224비트까지 줄였다. `x6` top-edge branch를 coarse bound에 반영해 대표 branch에서 `Z/Y`를 214비트대까지 낮췄고, branch sweep으로 12비트 prefix에서 `Z/Y=210`, 16비트 prefix에서 `Z/Y=206`, 20비트 sampled prefix에서 `Z/Y=202`까지 줄어드는 것을 확인했다. `T=784`는 `x6`를 46비트 tail로 분리하지만 polynomial slack 때문에 `Y=205`로 잡힌다. `T=924` Hensel prefix-only 12비트 limb CP-SAT는 product vars 686개 규모로 줄고 20초 probe에서 약 7천 branches/3천 conflicts까지 search에 진입했다. `T=848` exact-tail prototype은 tail low-low column 누락 버그를 수정한 뒤 정상 search에 진입했고, final carry zero, carry interval tightening, q interval lex bound, odd-residue filters, product-domain narrowing을 추가했다. `T=768` mixed-tail build/probe도 정상 search에 들어갔고, `p768=0`은 branches/conflicts `12852/3124`, `p768=1,q768=0`은 `6086/53`, `p768=1,q768=1`은 `9226/3582`로 boundary branch별 activity가 다르다. 대표 full-x6 후보 `0x24552149094`는 `T=848, tail_lims=75`에서 carry singleton `31/129`, max carry width 21비트까지 줄었지만 60초 probe는 아직 `UNKNOWN`이다. full-x6 후보 이후에는 `T=768/784/800/816/832/848` dynamic sweep이 필요하며, `T=784/800/816/832` short probe가 고정 `T=848`보다 active한 후보가 있다. 예를 들어 `x6=0x24552149097,T=784`는 conflicts `7820`, `x6=0x245521490bd,T=800`은 `4259`, `x6=0x2455214909b,T=832`는 `3938`이었다. q interval bound는 대표 x1low16 샘플에서 conflicts를 2983에서 7719로 늘렸고, odd-residue filters는 후보별 편차가 커서 shortlist 옵션으로 둔다. 현재 `--lowlift-q 265`는 q middle affine constraint이며, lower column을 제거해 carry를 넘기는 true `T=265` eliminated-carry 구현은 아니다. `cube_07_edge_driver.py`에 `tail848` ranking, fast-tail-only, JSONL/shard/tail-plan export, `x0/x7` branch sweep, 상위 cube T=848 exact-tail probe 경로, tail probe용 lowlift/odd-residue/q-interval/default-search plan 옵션을 추가했다. `run_07_tail_plan.py`로 dynamic tail plan을 병렬 실행하고 summary를 수집할 수 있다. 여러 `x7` branch를 유지한 beam sweep에서 대표 branch `x7=0`의 `x6_high` 후보가 36/40/44/46비트까지 확장됐고, heuristic full-x6 후보는 `0x24552149094`, `0x245521490bd` 등이었다. 이 후보들은 q prefix 255비트까지 늘리지만 T=848 exact-tail은 아직 `UNKNOWN`이다. `x6=0x245521490bd` 아래 `x1low16=0x00ff/0x11ff`가 grid sweep에서 가장 active했고, full-tail `T=784, tail_lims=79`에서도 각각 `UNKNOWN`이지만 `1958`, `1194` conflicts를 냈다. `x6=0x24552149098, x1low16=0xffff`가 broad grid에서 새 상위 후보였고, full-tail `T=784, tail_lims=79` 45초 probe에서도 `UNKNOWN`이지만 `4529` conflicts를 냈다. Z3 bit-vector tail-lock prototype도 추가했으나 같은 후보는 `tail_lims=16` 및 lower congruence only에서 5초 내 `unknown`이다. 고정 decision strategy를 빼면 branch/conflict가 늘어나므로 이후 probe는 기본 search도 함께 사용한다. 다만 한 `T=800` lowlift 샘플에서는 기본 search가 6초에 `6914/195`로 종료된 반면 p-decision probe가 시간 제한 정리를 오래 끌어 대량 sweep 기본값으로는 부적합할 수 있다. Go product-prefix CNF는 2048비트 product 폭까지 확장했고, 상수 adder simplification 후 대표 `T=800, arith_bits=1040` shortlist smoke가 `777622` vars / `3453804` clauses로 단일 tuple을 `UNSAT`로 닫았다. 다음 우선순위는 dynamic T sweep과 true `T=265` eliminated carry, optimized tail-column SAT CNF exporter다. 대체 branch로 `x7=4,x6=0cf4/0cfe`, `x7=5,x6=05d5/05df`도 큐에 유지한다. 전체 인수분해는 아직 미완료
- 7번 추가 tail probe: `x6=0x24552149098, x1low16=0xffff, T=784, tail_lims=79` randomized `seed=8`은 60초에 `UNKNOWN`, branches `66816`, conflicts `14067`이었다. `--lowlift-q 272` affine q-lift는 17개 limb constraint로 확장됐지만 대표 후보에서는 conflict activity가 낮아져 주력은 `265`를 유지한다. full-x6/x1low16 diversity probe에서도 `0x24552149098/0xffff`가 8초 conflicts `3223`으로 가장 active했다. `x1low24=0x22ffff`는 short probe top이었지만 full-tail 20초 conflicts `758`로 약했다. `T=768` small-tail enum은 p-tail assignment 후 q prefix를 재계산하도록 수정했고, 대표 후보에서 `p768=0,q768=1`이 4초에 `UNKNOWN`, conflicts `1032`로 가장 active했다. top80 dynamic short sweep에서는 모두 `UNKNOWN`이지만 `x6=0x245521490bd,x1low16=0x00ff,T=768`이 conflicts/sec `401.40`, `x6=0x24552149094,x1low16=0x00ff,T=800`이 `347.50`, `x6=0x24552149094,x1low16=0x66ff,T=784`가 `309.76`으로 active했다. `tail_lims=32` 재비교에서는 `x6=0x245521490bd,x1low16=0x77ff,T=784`가 12초 conflicts `2482`로 가장 active했고, full-tail `tail_lims=79` 기본 search 30초에서도 `UNKNOWN`이지만 conflicts `4093`, final carry zero enforced였다. `--compact-q-limbs`는 q lower BoolVar를 543개에서 0개로 줄이고, `x6=0x245521490bd,x1low16=0x77ff,T=784,tail_lims=79`에서 `--lowlift-q 272`와 결합 시 30초 conflicts `4861`로 baseline `4093`보다 약간 더 active했다. 따라서 `T=848` 고정보다는 낮은 `T`도 계속 섞고, full-x6 후보는 `0x24552149094`, `0x24552149097`, `0x24552149098`, `0x2455214909b`, `0x245521490bd`를 함께 유지한다. Z3 product-prefix oracle은 대표 cube 16개 모두 `unknown`이었다. 현재 환경은 `sage` CLI는 없지만 `python3`에서 `sage.all` import가 가능하고, `cuso`는 import되지 않는다.
- 7번 2026-06-02 추가 확인: folded Coron은 `x6_top=8,s=9`에서 raw margin `+8.00`이지만 primitive margin `-132.00`으로 여전히 실패권이고, `x1/x6` edge-fixed 가정은 primitive margin `+10.33`으로 verifier 가능권이다. 같은 cube `x6=0x245521490bd,x1low16=0x77ff,T=784,tail_lims=32,compact-q-limbs`에서 q interval bound는 conflicts/sec를 `55.72`에서 `153.10`으로 올렸지만, odd residue 8/16개는 각각 `65.07`, `40.23`으로 ranking 신호가 약했다. `--odd-residue-primes`에는 CP-SAT int64 domain guard를 추가했다. `T=768` mixed-tail 대표 cube는 `p768=1,q768=0`에서 conflicts/sec `383.66`으로 가장 active해 낮은 dynamic T sweep을 계속 유지한다.
- 7번 Go lowlift-q265 CNF: `go_hensel_tail`에 `--lowlift-q 265`를 추가해 `q[210..264] = Q0 + C*x1 (mod 2^55)` affine 제약을 Go/CNF exporter에도 넣었다. 의미 회귀 테스트는 `x1=0`과 `x1=2^39-1`에서 올바른 q-middle assignment는 unit propagation 모순이 없고 1비트 뒤집은 q-middle은 모순이 나도록 검사한다. free `x1/x2low7`, full `x6` 후보 5개, `T=784`, `arith_bits=272/320`, q interval, odd residues `3/5/7/11` 조합은 모두 SAT `5`, UNSAT `0`이었고, 여기에 `tail_window_bits=16`을 더해도 SAT `5`, UNSAT `0`이었다. 따라서 q-middle affine 제약은 fast Go path에 올렸지만, 후보 제거에는 true `T=265` eliminated-carry 또는 exact-tail carry-vector CNF가 필요하다.
- 7번 Go lowlift-q272 CNF: 같은 2-adic inverse 관계를 limb-aligned `q[0..271]` 전체로 확장해 `x1`뿐 아니라 `x2low7=p[265..271]`도 q-low에 직접 묶었다. 의미 회귀 테스트는 `x1=0x123456789a`, `x2low7=0x55`에서 기대 q-low 272비트 assignment가 unit propagation 모순 없이 통과하고 q-low 1비트 flip은 모순이 나는지 확인한다. free `x1/x2low7`, full `x6` 후보 5개, `T=784`, `arith_bits=272`, q interval, odd residues `3/5/7/11`는 SAT `5`, UNSAT `0`이고, `tail_window_bits=16`을 붙여도 SAT `5`, UNSAT `0`이었다. 즉 272 lift는 `x2low7`까지 연결하지만 여전히 lower carry-vector를 tail/product column으로 넘기지 않으면 pruning이 약하다.
- 7번 Go q272 noskip carry probe: `skip_known_prefix_bits=0`으로 낮은 product column을 실제 bit 0부터 계산해 보았다. five full-`x6` 후보, free `x1/x2low7`, `T=784`, q interval, odd residues `3/5/7/11`, `--lowlift-q 272`에서 `arith_bits=272`는 약 `63k` vars / `307k` clauses로 SAT `5`, UNSAT `0`, `arith_bits=320`은 약 `75k` vars / `361k` clauses로 SAT `5`, UNSAT `0`이었다. 즉 정확한 lower-prefix carry를 320비트까지 계산해도 free cube는 닫히지 않으며, 다음 SAT 작업은 broad free `x1`보다 작은 `x1/x2` cube 또는 더 깊은 exact-tail carry-vector가 필요하다.
- 7번 Go q272 small-cube noskip probe: `x6=0x245521490bd`, `x1low32=0xb183cdcc`, `x1high7=0x09`를 고정하고 `x2low7=0..127`만 assumption으로 훑었다. `T=784`, `skip_known_prefix_bits=0`, q interval, odd residues `3/5/7/11`, `--lowlift-q 272`에서 `arith_bits=320`은 SAT `128`, UNSAT `0`, 약 `63698` vars / `307366` clauses였고, `arith_bits=384`도 SAT `128`, UNSAT `0`, 약 `83731` vars / `399725` clauses였다. full `x1high7*x2low7` sweep은 180초 timeout이었다. 따라서 q272 noskip은 한 `x1high7` shard에서도 `x2low7`를 가르지 못하며, 다음 구현은 wider prefix보다 exact-tail carry-vector CNF 또는 더 깊은 cube가 우선이다.
- 7번 Go q272 512-bit shard probe: 같은 `x6=0x245521490bd`, `x1low32=0xb183cdcc`, `x1high7=0x09`, `x2low7=0..127` shard를 `arith_bits=512`까지 확장해도 SAT `128`, UNSAT `0`이었다. CNF는 약 `118028` vars / `559998` clauses이고 case별 assumptions는 14개였다. 이로써 해당 shard에서는 product-prefix 폭을 512비트까지 늘리는 방식도 `x2low7`를 자르지 못한다는 점을 확인했다.
- 7번 Go q272 640-bit endpoint probe: 같은 shard에서 전체 sweep 대신 `x2low7=0x00/0x7f` 두 endpoint만 `arith_bits=640`으로 검사했고 둘 다 SAT였다. CNF는 약 `215584` vars / `998039` clauses다. prefix 폭을 더 키워도 즉시 가지치기가 생기지 않으므로 exact-tail carry-vector 구현이 우선이다.
- 7번 Go exact carry-column prototype: `exact_tail_carry_limbs` / `--exact-tail-carry-limbs`와 `exact_carry_bits` / `--exact-carry-bits`를 추가했다. lower columns와 tail columns를 명시적 carry vector로 연결하는 정확한 scaffold이고 toy product 회귀는 통과했다. `--build-only`도 추가해 export와 solve 비용을 분리했다. assumption-base endpoint는 build-only에서도 180초 timeout이고, fixed endpoint는 q272 known-mask 승격과 skip-prefix carry 적용 후 `exact_carry_bits=272`에서 처음 `404939` vars / `1802414` clauses였다. column별 carry-width 상한을 적용한 뒤에는 `385511` vars / `1744108` clauses, known limb product constant-folding 뒤에는 `380675` vars / `1727237` clauses로 줄었지만 solve는 180초 timeout이다. 현재 형태는 correctness guardrail이지 빠른 pruning oracle은 아니다.
- 7번 Go free-mode dynamic T: `run_07_go_sat_filter.py`의 `--T-candidates`를 `--free-x1-filter`와 `--free-x1-x6high-filter`에도 허용했다. full `x6=0x24552149094` smoke는 `T=784/800` 둘 다 SAT였고, `x6high=0x91548524`, `x6high_bits=36` smoke는 `T=784`가 fixed high boundary 794 때문에 `skipped_t`로 기록되고 `T=800`만 SAT였다. 이 패치는 새 pruning은 아니지만, free/broad candidate generation에서도 cube별 dynamic T를 비교한 뒤 exact-tail carry-vector 후보로 넘길 수 있게 한다.
- 7번 Go odd-residue compression: residue automaton이 known bit를 상수 shift로 접고 unknown bit에서만 상태를 만들도록 바꿨다. full-known toy는 state var/clauses가 추가되지 않고, one-unknown-bit toy는 residue state가 추가되는 회귀 테스트를 넣었다. 대표 free dynamic-T smoke에서 odd residue `3` 포함 CNF가 full `x6=0x24552149094,T=784` 기준 `15518/54398`에서 `12116/46452` vars/clauses로, `T=800` 기준 `15551/54430`에서 `12149/46484`로 줄었다. `x6high=0x91548524,T=800`도 `15551/54550`에서 `12254/46849`로 줄고 SAT/skip 결과는 유지됐다.
- 7번 Go compressed free sweep: 압축 후 direct free-`x1` 필터를 5개 full `x6` 후보 `0x24552149094/097/098/09b/0bd`와 `T=784/800/816/832/848` 전체에 적용했다. 조건은 `arith_bits=272`, `skip_known_prefix_bits=210`, `--lowlift-q 272`, q interval, odd residues `3/5/7/11`이다. 25개 candidate/T pair가 모두 SAT였고, CNF 크기는 약 `33.2k` vars / `170k` clauses였다. 따라서 compressed odd-residue는 비용 최적화일 뿐 pruning oracle은 아니며, 다음 구조 개선은 exact-tail carry-vector 또는 더 깊은 `x1/x2` cube다.
- 7번 2026-06-02 short sweep: full-x6 후보 5개와 `x1low16` 대표 16개를 `T=768/784/800`, `tail_lims=32`로 재검사했다. 상위는 `x6=0x245521490bd,x1low16=0x00ff,T=784`의 conflicts/sec `406.95`, `x6=0x245521490bd,x1low16=0x22ff,T=768,p768=1,q768=1`의 `376.55`, `x6=0x24552149094,x1low16=0x00ff,T=768,p768=1,q768=0`의 `336.54`였다. 상위 두 개 full-tail 확장은 각각 `UNKNOWN`이지만 final carry zero 상태에서 conflicts `7109`, `5741`까지 search가 진행됐다.
- 7번 cuso fixed-range 재시도: `solve_07_cuso.py`에 `--fix-p-range`를 추가해 CP-SAT shortlist를 cuso unknown-divisor 모델에 직접 넣을 수 있게 했다. 대표 `x6=0x245521490bd,x1low16=0x00ff,x0=x7=0`은 남은 변수 bounds `[23,84,78,69,87]`까지 줄었지만, no-graph/max-shifts 96/multiplicity 1 probe에서 HHG integer relation 0개였다. `x1low16=0x22ff`와 recenter/weight 변형도 같은 결과다.
- 7번 q-decision 추가: `try_07_hensel_tail_cp_sat.py`에 `--decision-q-range`, `cube_07_edge_driver.py`에 `--tail-decision-q-range`를 추가했다. `x6=0x245521490bd,x1low16=0x00ff,T=784,tail_lims=32,compact-q-limbs`에서 q high-side `720:64` decision은 conflicts/sec를 `406.95`에서 `907.04`로 올렸다. full-tail에서도 `T=784` 후보는 30초 conflicts `7394`, `T=768,x1low16=0x22ff,p768=1,q768=1` 후보는 60초 conflicts `19529`까지 진행했지만 둘 다 아직 `UNKNOWN`이다.
- 7번 q-decision full-tail 재랭킹: full-x6 후보 5개와 `x1low16` 대표 16개를 `T=768/784/800`, `tail_lims=32`, `compact-q-limbs`, q-decision `704:64`로 재검사했다. short top은 `x6=0x245521490bd,x1low16=0x66ff,T=784`의 conflicts/sec `1909.41`, `x6=0x24552149094,x1low16=0x44ff,T=800`의 `1733.73`, `x6=0x245521490bd,x1low16=0x77ff,T=784`의 `1679.94`였다. full-tail 확장에서는 `x6=0x245521490bd,x1low16=0x66ff,T=800,tail_lims=78,decision-q-range 704:64`가 45초 conflicts `26962`, 90초 conflicts `37708`, randomized seed 8 60초 conflicts `32647`로 현재 best activity지만 모두 `UNKNOWN`이다. 같은 cube에서 `decision-q-range 720:64`는 60초 conflicts `19519`로 약했다. 이 결과는 `T=800` dynamic boundary를 shortlist에 올리고, CP-SAT 장기 단일 실행보다 CNF/assumption 기반 병렬 cube-and-conquer로 옮기는 쪽을 지지한다. `go_hensel_tail` scaffold를 추가하고 `go test ./...`로 확인했다.
- 7번 x1low24 추가 sweep: `try_07_hensel_tail_cp_sat.py`에 `--decision-select min|max`를 추가했다. best cube에서 `max` 단독은 45초 conflicts `10905`로 약했고, p+q decision 결합은 `22604`, max+randomized seed 11은 `22588`이었다. 기존 q704/min 45초 `26962`가 여전히 우세하다. `x6=0x245521490bd,x1low16=0x66ff,T=800` 아래에서 `x1low24=0x??66ff` 256개를 `tail_lims=32`, 3초로 sweep했고 모두 `UNKNOWN`이었다. 상위는 `0x8466ff` conflicts/sec `1857.95`, `0x6166ff` `1740.40`, `0x1b66ff` `1727.64`였다. full-tail 확장은 각각 45초 `UNKNOWN`이고 conflicts는 `0x8466ff` `27440`, `0x6166ff` `27345`, `0x1b66ff` `26503`이다. `x1low24` 고정은 `q_low_known_bits`를 226에서 234로 올리지만 아직 닫히지 않는다. 같은 `x1low24` 후보를 cuso fixed-range에 넣으면 remaining bounds `[15,84,78,69,87]`까지 줄지만 max-shifts 128에서도 HHG integer relation 0개다.
- 7번 x1low32/full-x1 추가 sweep: `x6=0x245521490bd,x1low24=0x8466ff,T=800` 아래에서 `x1low32=0x??8466ff` 256개를 짧게 훑었고 `0xd08466ff`가 conflicts/sec `1635.46`으로 가장 active했다. 이를 full `x1=(hi7<<32)|0xd08466ff` 128개로 확장하면 상위는 `0x7ad08466ff` conflicts/sec `1616.18`, `0x7bd08466ff` `1441.58`, `0x7ed08466ff` `1389.94`였다. 상위 두 full `x1` 후보를 full `x6=0x245521490bd`와 함께 edge-fixed projected Coron verifier에 넣었고 둘 다 primitive margin `10.67`이지만 `k=7`, groebner root extraction에서 factor를 찾지 못했다. 따라서 CP-SAT conflict/sec는 queue ranking 신호일 뿐 정답성 검증은 아니며, 다음 구조적 개선은 Python 장기 CP-SAT보다 Go/Rust 기반 Hensel-tail CNF exporter와 SAT assumption 병렬 cube-and-conquer다. 현재 Go scaffold는 JSON/limb/column/DIMACS unit clause까지 있고 산술 column equality CNF 인코딩은 아직 없다.
- 7번 x1low32/full-x1 두 번째 base: `x1low32=0xc68466ff`도 full `x1` 128개로 확장했다. 모두 `UNKNOWN`이고 상위는 `0x60c68466ff` conflicts/sec `1590.44`, `0x5cc68466ff` `1460.21`, `0x35c68466ff` `1442.90`이었다. `x1=0x60c68466ff,x6=0x245521490bd`의 projected Coron verifier도 primitive margin `10.67`이지만 factor를 찾지 못했다. Go `go_hensel_tail`은 q common-prefix 자동 계산과 p/q tail-known 검사를 추가해 `T=800,x6=0x245521490bd,x1=0x60c68466ff`에서 q prefix start `769`, p/q tail unknown bits `0/0`, p low unknown bits `318`, q low fixed bits `296`으로 Python CP-SAT 모델과 같은 구조를 진단한다. 다음 Go 단계는 lower recurrence/exact-tail limb equality를 CNF로 내보내는 것이다.
- 7번 lower-prefix/carry-seed 추가: full `x1=0x7ad08466ff`, full `x6=0x245521490bd`에서 Z3 Hensel prefix를 재검사했다. x0/x7를 빼먹으면 q low가 150비트로 떨어져 `check_bits=272`가 전부 `unknown`이므로 잘못된 진단이고, x0=x7=0을 고정하면 q low가 273비트까지 올라가 `x2low8` 16개 sample은 모두 `sat`다. `check_bits=380`은 `x2low16` 8개 sample 모두 `unknown`이다. CP-SAT tail model에는 `--skip-known-prefix-limbs`를 추가해 full x1 + x2[265..271] 고정 시 carry_272를 상수로 계산하고 lower columns 0..271을 건너뛸 수 있게 했다. sample cube에서는 `skipped_prefix_carry=207274`, lower constant terms `438 -> 285`지만 product var 수는 그대로 `121`이라 5초 activity 이득은 작다. 그래도 `x2low7` 전체 sweep에서 nonzero-conflict 후보가 23/128로 갈렸고, top은 `x2low7=0x11` conflicts/sec `339.92`였다. 이를 full-tail로 밀면 `UNKNOWN`, 64.06초, conflicts `10339`, final carry zero 상태다.
- 7번 x2low7 두 번째 후보: `cube_07_edge_driver.py`에도 `--tail-skip-known-prefix-limbs`를 연결해 x2low7 fixed range와 skip-prefix carry seed를 tail plan으로 만들 수 있게 했다. 두 번째 active full-x1 `0x60c68466ff` 아래에서 x2low7 128개를 sweep하면 nonzero-conflict 후보가 57/128이고, top은 `0x7e` conflicts/sec `397.22`, `0x7b` `392.75`, `0x43` `387.53`이다. 첫 번째 full-x1의 top `0x11/0x2b/0x26`과 거의 겹치지 않으므로 x2low7은 x1 후보별 ranking 신호로 관리해야 한다. `x1=0x60c68466ff,x2low7=0x7e,x6=0x245521490bd` full-tail은 `UNKNOWN`, 76.22초, conflicts `12234`, final carry zero 상태다.
- 7번 x2low7 Coron verifier 연결: `solve_07_hybrid_coron.py`에 `--fix-p-range`를 추가해 `x2[265..271]` 같은 추가 p fixed range를 folded Coron에도 반영한다. full `x1/x6`만 고정하면 `low=265`, `Xbits=Ybits=504`, primitive margin `10.67`인데, `x2low7`까지 고정하면 `low=272`, `Xbits=Ybits=497`, primitive margin `20.00`으로 올라간다. `x1=0x7ad08466ff,x2low7=0x11,x6=0x245521490bd`는 projected Coron k=6/k=7에서 각각 not found, `x1=0x60c68466ff,x2low7=0x7e,x6=0x245521490bd`는 k=6에서 not found다. low-boundary 개선은 정상 작동하지만 CP-SAT activity top 후보가 정답이라는 증거는 아직 없다.
- 7번 x2low7 verifier 추가: `x1=0x60c68466ff,x2low7=0x7e,x6=0x245521490bd`는 projected Coron k=7에서도 not found였다. 같은 `x1/x6`에서 x2low7 sweep 상위권인 `0x7b`, `0x43`도 k=6 not found다. `x1=0x7ad08466ff,x2low7=0x2b,x6=0x245521490bd`는 k=6/k=7 모두 not found다. 즉 `x2low7` 고정으로 primitive margin을 `+20`까지 올리는 것은 verifier 조건을 개선하지만, CP-SAT conflict/sec 상위 후보 자체가 정답성을 보장하지 않는다.
- 7번 full `x1/x6/x2low7` dynamic T sweep: `x6=0x245521490bd,x1=0x60c68466ff,x2low7=0x7e`는 5초 probe 기준 `T=784`가 conflicts/sec `1333.61`로 가장 active했고 `T=800/816`이 약 `1161`, `T=848`은 `869.70`이었다. 반면 `x1=0x7ad08466ff,x2low7=0x2b`는 `T=800`이 `1080.40`으로 가장 active하고 `T=784`는 `783.55`에 그쳤다. full x6 후보가 생긴 뒤에도 `T=848` 고정은 부적합하며, 각 cube마다 `T=784/800/816/832/848` short sweep으로 장기 probe 대상을 고르는 것이 더 낫다.
- 7번 Go 경로 판단: Go는 Coppersmith/JM/Sage verifier를 대체하기보다 Hensel-tail hot path, 즉 cube scoring, dynamic T sweep, DIMACS/assumption export에 쓰는 것이 맞다. 현재 Go scaffold는 q prefix/tail-known 진단과 column metadata까지 신뢰할 수 있으므로, 다음 구현은 산술 column equality CNF와 solver assumptions 출력이다. Python/OR-Tools 장기 단일 실행은 후보 ranking에는 유용하지만 실제 cube-and-conquer에는 Go 또는 Rust CNF exporter가 더 적합하다.
- 7번 Go CNF 첫 산술 경로: `go_hensel_tail`에 `arithmetic_bits` / `--arith-bits`를 추가했다. 현재는 최적화된 tail column equality가 아니라 `p*q == N (mod 2^k)` lower-prefix를 bit-level AND/XOR/full-adder CNF로 내보내는 smoke exporter다. 대표 shortlist cube `T=784,x1=0x60c68466ff,x2low7=0x7e,x6=0x245521490bd`에서 `arith_bits=64`는 `3136` vars / `7809` clauses, `arith_bits=128`은 `7755` vars / `29356` clauses로 생성됐다. PATH에는 `kissat/cadical/minisat`가 없어 solve까지는 못 했고, 다음 Go 단계는 이 prefix encoder를 tail-lock column equality와 assumptions export로 확장하는 것이다.
- 7번 Go CNF tail prefix 확장: `arith_bits`가 `T`를 넘을 수 있게 바꿔 `T` 이상 known p/q high-tail bit도 product-prefix CNF에 포함한다. 또한 `--skip-known-prefix-limbs`를 연결해 bit 0..271처럼 완전히 known인 lower prefix product를 상수 carry로 접는다. 같은 대표 cube에서 `arith_bits=800`은 `442468` vars / `2013402` clauses이고, `skip_known_prefix_limbs=17`을 적용하면 `390569` vars / `1771380` clauses로 줄었다. 아직 full optimized tail equality보다는 크지만, 실제 tail bit를 포함하는 SAT 입력이 생성되기 시작했다.
- 7번 Go CNF + PySAT 필터: 시스템에는 `kissat/cadical/minisat`가 없고 system Python pip는 PEP 668로 막혀 있어 `/tmp/cryptotest_sat_venv` venv에 `python-sat`을 설치했다. `run_07_go_sat_filter.py`를 추가해 Go DIMACS exporter와 PySAT CaDiCaL solve를 연결했다. 낮은 `arith_bits=128` smoke는 SAT였지만, `T=784,arith_bits=800,skip_known_prefix_limbs=17,x6=0x245521490bd` product-prefix CNF는 CP-SAT shortlist 8개를 모두 UNSAT로 제거했다: `x1=0x60c68466ff`의 `x2low7=0x7e/0x7b/0x43/0x7d/0x47`, `x1=0x7ad08466ff`의 `x2low7=0x11/0x2b/0x26`. 각 CNF는 대략 `386k..391k` vars, `1.75M..1.77M` clauses였다. 즉 CP-SAT conflict activity 상위 후보를 실제 product-prefix SAT 필터로 탈락시키는 데 성공했다.
- 7번 Go/PySAT batch 확장: `run_07_go_sat_filter.py`에 `--case-file`과 1회 Go binary build 재사용을 추가했다. 같은 `T=784,arith_bits=800,skip_known_prefix_limbs=17` 필터로 추가 full-x1 후보 `0x7bd08466ff`, `0x7ed08466ff`, `0x5cc68466ff`, `0x35c68466ff`와 active `x2low7` 조합 20개를 모두 UNSAT로 제거했다. 또한 다른 full-x6 후보 `0x24552149094/097/098/09b`에 대해 대표 `(x1,x2low7)=(0x60c68466ff,0x7e),(0x7ad08466ff,0x11)` 8개도 모두 UNSAT였다. 현재까지 Go/PySAT product-prefix 필터 결과는 SAT `0`, UNSAT `36`이다.
- 7번 Go/PySAT assumptions sweep: `go_hensel_tail`에 `-var-map` JSON export를 추가하고 `run_07_go_sat_filter.py`에 `--assume-x2low7`을 추가했다. 이제 `x1/x6` 고정 CNF 하나를 만든 뒤 `p_265..p_271` assumptions 7개로 여러 `x2low7` 값을 검사한다. smoke에서 fixed-CNF와 assumptions-CNF가 같은 SAT 결론을 냈고, `T=784,arith_bits=800,skip_known_prefix_limbs=16,x6=0x245521490bd`로 기존 top `x2low7` 8개를 모두 UNSAT로 재확인했다. 공유 CNF 크기는 `x1=0x60c68466ff`에서 `405382` vars / `1835554` clauses, `x1=0x7ad08466ff`에서 `405816` vars / `1837570` clauses였다.
- 7번 full `x2low7` assumption sweep: `run_07_go_sat_filter.py`에 `--x1`과 `--x2low7-all`을 추가했다. `x6=0x245521490bd` 아래 full `x1` 후보 `0x60c68466ff`, `0x7ad08466ff`, `0x7bd08466ff`, `0x7ed08466ff`, `0x5cc68466ff`, `0x35c68466ff` 각각에 대해 `x2low7=0..127` 전체를 검사했고 768개 모두 UNSAT였다. 다른 full-x6 대표 `0x24552149094/097/098/09b`와 대표 full-x1 `0x60c68466ff`, `0x7ad08466ff` 조합도 1024개 모두 UNSAT였다. 새 assumptions sweep 총계는 SAT `0`, UNSAT `1792`다.
- 7번 추가 full `x2low7` assumption sweep: 기존 full-x1 sweep 로그에서 아직 검사하지 않은 상위 full-x1 후보 `0x18c68466ff`, `0x2ad08466ff`, `0x3fc68466ff`, `0x7fd08466ff`, `0x7cd08466ff`, `0x57c68466ff`, `0x2cc68466ff`, `0x4ad08466ff`를 `x6=0x245521490bd`에서 검사했고 1024개 모두 UNSAT였다. 현재 full `x2low7` assumptions sweep 누계는 SAT `0`, UNSAT `2816`이다.
- 7번 `x6=0x24552149098` bucket 확장: 기존 activity 후보 `x1low16=0xffff`, `x1low24=0x22ffff`에서 `x1low32=0x??22ffff` 256개를 sweep했고 상위는 `0x0c22ffff`, `0xfa22ffff`, `0xfb22ffff`였다. 상위 두 base를 full `x1` 256개로 확장하면 `0x190c22ffff`, `0x30c22ffff`, `0x140c22ffff`, `0x0c22ffff`, `0x7bfa22ffff` 등이 conflict/sec 상위였다. 그중 상위 full `x1` 8개를 Go/PySAT `x2low7=0..127` assumptions filter에 넣었고 1024개 모두 UNSAT였다. 현재 full `x2low7` assumptions sweep 누계는 SAT `0`, UNSAT `3840`이다.
- 7번 `x6=0x24552149094` bucket 확장: `x1low16=0x44ff` 아래 `x1low24=0xfd44ff/0xfc44ff`가 short probe 상위였고, `x1low32` 상위는 `0xc4fd44ff`, `0x12fd44ff`, `0xdbfd44ff` 등이었다. 상위 full `x1` 8개는 Go/PySAT `x2low7=0..127` assumptions filter에서 1024개 모두 UNSAT였다. 새 `--assume-x1high7-x2low7` 모드로 `x1low32=0xc4fd44ff`와 추가 top7 base `0x12fd44ff`, `0xdbfd44ff`, `0x5cfd44ff`, `0x41fd44ff`, `0x42fd44ff`, `0x60fd44ff`, `0x3bfd44ff`를 각각 상위 7비트와 `x2low7` 전체 공간으로 검사했고, 총 `8 * 128 * 128 = 131072`개가 모두 UNSAT였다. 이전 full-`x1` top8 중 중복을 제외한 새 제거 조합은 `131456`개이며, Go/PySAT assumptions 기반 필터 누계는 최소 SAT `0`, UNSAT `135296`개다. 따라서 CP-SAT full-`x1` ranking보다 `x1low32` base-wide SAT assumption sweep을 더 이른 단계에 넣는 쪽이 우선이다.
- 7번 추가 base-wide Go/PySAT sweep: `x6=0x24552149098`의 top5 `x1low32` base `0x0c22ffff`, `0xfa22ffff`, `0xfb22ffff`, `0xd922ffff`, `0xda22ffff`를 전부 검사했고 `81920`개 모두 UNSAT였다. `x6=0x245521490bd`의 top5 `0xd08466ff`, `0xc68466ff`, `0x868466ff`, `0x8d8466ff`, `0x538466ff`도 `81920`개 모두 UNSAT였다. 남은 대표 `x6=0x24552149097/09b`에는 공통 base `0xc4fd44ff`, `0x0c22ffff`, `0xd08466ff`, `0xc68466ff`를 교차 적용했고 `131072`개 모두 UNSAT였다. 이번 raw 추가 row는 `294912`개, 보수적 unique 추가 제거는 최소 `291584`개이며, Go/PySAT assumptions 기반 필터 누계는 최소 SAT `0`, UNSAT `426880`개다. 이제 후보 생성은 CP-SAT conflict/sec full-`x1` ranking보다 `x1low32` base를 바로 SAT assumptions로 걸어 넓게 제거하는 방식이 중심이다.
- 7번 추가 next-base Go/PySAT sweep: 아직 검사하지 않은 상위 `x1low32` base를 `x6=0x24552149094/098/bd`에서 각각 8개씩 더 검사했다. `0x49094`는 `0x4dfd44ff`, `0x68fd44ff`, `0x2afd44ff`, `0x97fd44ff`, `0x2cfd44ff`, `0x56fd44ff`, `0x20fd44ff`, `0x65fd44ff`, `0x49098`은 `0xde22ffff`, `0xf522ffff`, `0xa722ffff`, `0xf922ffff`, `0xcf22ffff`, `0xc722ffff`, `0xd422ffff`, `0xab22ffff`, `0x490bd`는 `0xe48466ff`, `0xfd8466ff`, `0xce8466ff`, `0xe98466ff`, `0xd28466ff`, `0x8c8466ff`, `0xeb8466ff`, `0x6a8466ff`를 검사했다. 총 `393216`개 모두 UNSAT였고, 보수적 Go/PySAT assumptions 누계는 SAT `0`, UNSAT `820096`개다. conflict/sec 상위 base가 계속 탈락하므로 같은 x6 bucket을 깊게 파는 것과 동시에 x6 후보 자체를 다시 다양화해야 한다.
- 7번 대체 `x7/x6` branch 확장: `x7=4,x6_high=0cf4/0cfe`와 `x7=5,x6_high=05d5/05df`를 `fast-tail-only`로 20 -> 28 -> 36 -> 44 -> 46비트까지 확장했다. 상위 full-x6 후보 중 `x7=4`의 `0x033d24200413`, `0x033d2420043c`, `0x033d24200465`, `0x033d2420048e`와 `x7=5`의 `0x017554242005`, `0x01755424202c`, `0x01755424202e`, `0x017554242055`를 대표 `x1low32` base 6개와 교차해 Go/PySAT `--assume-x1high7-x2low7`로 검사했다. 추가 `393216`개 모두 UNSAT였고, 보수적 Go/PySAT assumptions 누계는 SAT `0`, UNSAT `1213312`개다. 대체 branch도 현재 대표 base와는 모두 모순이므로, heuristic queue는 계속 다양하게 유지해야 한다.
- 7번 full `x7` diversity sweep: global tail848 top이 `x7=0`에 쏠리는 것을 피하기 위해 `x7=1..15` 각각에서 16비트 `x6_high` top을 따로 생성했고, 아직 넓게 검사하지 않았던 `x7=1/2/3/6`을 full 46비트 `x6` 후보까지 확장했다. 상위 후보 `0x00010018000f`, `0x000100180038`, `0x00014004100e`, `0x000140041028`, `0x000100040818`, `0x000100040841`, `0x0002401c0c0a`, `0x0002401c0c24`를 대표 `x1low32=0xc4fd44ff/0xd08466ff`와 교차해 검사했고 추가 `262144`개 모두 UNSAT였다. 보수적 Go/PySAT assumptions 누계는 SAT `0`, UNSAT `1475456`개다. q-prefix/tail848 ranking만으로 후보를 깊게 파는 것은 계속 탈락하므로, 다음 우선순위는 Go CNF의 optimized Hensel-tail column equality와 assumptions 기반 cube-and-conquer다.
- 7번 Go CNF q interval bound: `go_hensel_tail`에 `q_interval_bound` / `--q-interval-bound`를 추가했다. 현재 p interval에서 `q_min..q_max`를 계산하고, known `qH`를 뺀 뒤 `qL_min <= qL <= qL_max`를 unsigned lex CNF bound로 넣는다. 대표 cube에서 `arith_bits=272`는 vars `2951` 그대로 clauses `7045 -> 7561`, `arith_bits=800`은 vars `434011` 그대로 clauses `1959285 -> 1959801`이었고, 결과는 동일하게 UNSAT였다. `x6=0x24552149094,x1low32=0xc4fd44ff`의 q-bound full assumptions smoke `16384`개도 모두 UNSAT였다.
- 7번 q-bound 추가 sweep: 기존 주요 `x6=0x24552149094/098/bd` bucket에서 아직 검사하지 않은 `x1low32` base를 4개씩 골라 `--q-interval-bound`, `arith_bits=272`, `skip_known_prefix_limbs=15`로 Go/PySAT base-wide assumptions sweep을 돌렸다. `0x49094`는 `0x1dfd44ff`, `0x35fd44ff`, `0x8ffd44ff`, `0x82fd44ff`, `0x49098`은 `0xfe22ffff`, `0xbc22ffff`, `0x2d22ffff`, `0x8122ffff`, `0x490bd`는 `0x798466ff`, `0x1e8466ff`, `0xab8466ff`, `0xd68466ff`를 검사했고 추가 `196608`개 모두 UNSAT였다. 보수적 Go/PySAT assumptions 누계는 SAT `0`, UNSAT `1671064`개다.
- 7번 x6-low assumptions 경로: `run_07_go_sat_filter.py`에 `--assume-x6low-x1high7-x2low7`을 추가했다. `T=800` 이상에서 `x6` low bits가 lower part 안에 남는 것을 이용해, base CNF에는 `x6` high prefix와 `x1low32`만 고정하고 `x6low`, `x1high7`, `x2low7`을 PySAT assumptions로 훑는다. Smoke로 `x6high_bits=44`, `x6high=0x9154852425`, `x1low32=0xc4fd44ff`, `x1high7=0x61`, `x2low7=0x00`을 검사했고 low2 네 값 `0x24552149094..97`이 모두 UNSAT였다. 이 결과는 특정 `x1high7/x2low7` 조합 smoke라 누계에는 넣지 않지만, full `x6` 후보 고정 편향을 줄이는 경로가 생겼다.
- 7번 high40 x6-low sweep: 주요 full-x6 후보 `0x24552149094/098/bd`가 모두 high40 prefix `0x915485242`를 공유하므로, `x6high_bits=40`, `T=800`에서 low6 전체를 assumptions로 훑었다. `x1low32=0xd08466ff`, `x1high7=0x60`, `x2low7=0x7e` smoke는 low6 `64`개 모두 UNSAT였고, 같은 high40 prefix에서 `x2low7=0x7e`를 고정한 뒤 `x1high7=0..127`과 `x6low6=0..63` 총 `8192`개도 모두 UNSAT였다. 특정 `x2low7` prefix-sweep이므로 보수 누계에는 넣지 않았지만, high40 prefix 아래 full-x6 후보 64개를 한 CNF에서 처리하는 경로가 검증됐다.
- 7번 high40 x6-low multi-x2 sweep: 같은 `x6high_bits=40`, `x6high=0x915485242`, `x1low32=0xd08466ff`에서 `x2low7` 후보 `0x7e/0x7b/0x43/0x11/0x2b` 다섯 개를 함께 검사했다. `x6low6=0..63`, `x1high7=0..127`까지 포함해 `64 * 128 * 5 = 40960`개 모두 UNSAT였다. 이 prefix-sweep도 특정 base에 한정되므로 보수 누계에는 넣지 않지만, `0xd08466ff` base와 해당 high40 `x6` prefix의 우선순위는 낮아졌다.
- 7번 high36 x6-low sweep: 같은 주요 full-x6 후보들은 high36 prefix `0x91548524`도 공유하므로, `x6high_bits=36`, `T=800`, q interval bound를 켜고 low10 전체를 assumptions로 훑었다. `x1low32=0xd08466ff,x1high7=0x60`과 `x1low32=0xc4fd44ff,x1high7=0x61` 두 대표 base에서 각각 `x2low7=0x7e/0x7b/0x43/0x11/0x2b`를 검사했고, 각 sweep `1024 * 5 = 5120`개가 모두 UNSAT였다. 이 결과도 fixed `x1high7` prefix-sweep이라 보수 누계에는 넣지 않지만, high36 prefix 아래 full-x6 후보 1024개를 한 CNF assumptions로 처리하는 경로가 검증됐다.
- 7번 high32 x6-low sweep: 주요 full-x6 후보가 들어 있는 high32 prefix `0x9154852`에서도 low14 전체를 assumptions로 훑었다. `x1low32=0xd08466ff,x1high7=0x60,x2low7=0x7e`와 `x1low32=0xc4fd44ff,x1high7=0x61,x2low7=0x7e` 두 smoke가 각각 `16384`개 모두 UNSAT였다. fixed `x1high7/x2low7` smoke라 보수 누계에는 넣지 않지만, high32 bucket 아래 full-x6 후보 16384개를 한 CNF assumptions로 처리하는 경로가 검증됐다.
- 7번 high32 x1high shard sweep: 같은 high32 prefix `0x9154852`, 기준 `x2low7=0x7e`에서 `x1high7=0x60..0x63` 네 값 shard를 검사했다. `x1low32=0xd08466ff`와 `0xc4fd44ff` 두 base가 각각 `16384 * 4 = 65536`개 모두 UNSAT였다. 이 역시 fixed `x2low7` shard라 보수 누계에는 넣지 않지만, high32 bucket에서 `x1high7` shard 단위 assumptions sweep이 충분히 빠르게 동작함을 확인했다.
- 7번 Go CNF odd-residue option: `go_hensel_tail`과 `run_07_go_sat_filter.py`에 repeatable `--odd-residue-prime`을 추가했다. 각 작은 홀수 modulus에 대해 `p mod r`, `q mod r` residue automaton을 만들고 `p*q != N (mod r)` residue pair를 금지한다. Smoke에서 `--odd-residue-prime 3 --odd-residue-prime 5`, `arith_bits=272`는 high44 low2 네 값을 모두 UNSAT로 유지했다. 그러나 high32 `x1high7=0x60..0x63` shard는 `arith_bits=272 + q_interval_bound`만으로도 `65536`개 모두 UNSAT였고, odd 3/5를 추가해도 결론은 같지만 CNF가 `2953/7423` vars/clauses에서 `19353/61761`로 커졌다. 따라서 odd-residue는 기본값이 아니라 shortlist 옵션으로 둔다.
- 7번 high32 weak-filter shard: 같은 high32 prefix `0x9154852`, 기준 `x2low7=0x7e`에서 다음 `x1high7=0x64..0x67` shard도 약한 `arith_bits=272 + q_interval_bound`로 검사했다. `x1low32=0xd08466ff`와 `0xc4fd44ff` 두 base가 각각 `65536`개 모두 UNSAT였다. 이제 이 bucket에서는 expensive `arith_bits=800` 전에 `arith_bits=272` shard prefilter를 먼저 쓰는 편이 낫다.
- 7번 high32 weak-filter shard 확장: 같은 조건에서 `x1high7=0x68..0x6f` shard를 추가 검사했고, `x1low32=0xd08466ff`와 `0xc4fd44ff` 두 base가 각각 `131072`개 모두 UNSAT였다. 따라서 high32 `0x9154852`, 기준 `x2low7=0x7e`에서는 두 대표 base의 `x1high7=0x60..0x6f` 총 16개 high shard, 두 base 합계 `524288`개 조합이 약한 `arith_bits=272` prefilter에서 제거됐다.
- 7번 high32 weak-filter shard 마감: 같은 조건에서 `x1high7=0x70..0x7f` shard도 검사했고, `x1low32=0xd08466ff`와 `0xc4fd44ff` 두 base가 각각 `262144`개 모두 UNSAT였다. 따라서 high32 `0x9154852`, 기준 `x2low7=0x7e`에서는 두 대표 base의 `x1high7=0x60..0x7f` 전체가 닫혔고, 두 base 합계 `1048576`개 조합이 약한 `arith_bits=272` prefilter에서 제거됐다. 특정 `x2low7`/bucket 한정이라 전체 보수 누계에는 넣지 않는다.
- 7번 high32 weak-filter x2 확장: 같은 high32 `0x9154852` bucket과 `x1high7=0x60..0x7f` 범위를 두 번째 `x2low7=0x7b` 후보에도 적용했다. `x1low32=0xd08466ff`와 `0xc4fd44ff` 두 base가 각각 `524288`개 모두 UNSAT였다. 따라서 이 bucket의 두 대표 base는 `x2low7=0x7e/0x7b`에 대해 두 base 합계 `2097152`개 조합이 약한 `arith_bits=272` prefilter에서 제거됐다.
- 7번 high32 weak-filter x2 추가 확장: 같은 조건에서 세 번째 `x2low7=0x43` 후보도 검사했고, `x1low32=0xd08466ff`와 `0xc4fd44ff` 두 base가 각각 `524288`개 모두 UNSAT였다. 따라서 high32 `0x9154852`, `x1high7=0x60..0x7f` 범위에서 두 대표 base는 `x2low7=0x7e/0x7b/0x43` 세 후보에 대해 두 base 합계 `3145728`개 조합이 약한 `arith_bits=272` prefilter에서 제거됐다.
- 7번 high32 weak-filter 대표 x2 shortlist 마감: 같은 조건에서 남은 대표 `x2low7=0x11/0x2b`도 검사했고, 두 x2 후보 각각 두 base 합계 `1048576`개가 모두 UNSAT였다. 따라서 high32 `0x9154852` bucket의 두 대표 base는 `x2low7={0x7e,0x7b,0x43,0x11,0x2b}` 전부에서 `x1high7=0x60..0x7f` 전체가 닫혔고, 두 base 합계 `5242880`개 조합이 약한 `arith_bits=272` prefilter에서 제거됐다.
- 7번 high32 weak-filter 추가 base: 이전 conflict ranking 상위였던 `x1low32=0x0c22ffff`, `0xfa22ffff`, `0xfb22ffff`, `0xd922ffff`, `0xda22ffff`, `0xfe22ffff`, `0xbc22ffff`, `0x2d22ffff`, `0x8122ffff`, `0xde22ffff`, `0xf522ffff`, `0xa722ffff`, `0xf922ffff`, `0xcf22ffff`, `0xc722ffff`, `0xd422ffff`, `0xab22ffff`, `0x12fd44ff`, `0xdbfd44ff`, `0x5cfd44ff`, `0x41fd44ff`, `0x42fd44ff`, `0x60fd44ff`, `0x3bfd44ff`, `0x4dfd44ff`, `0x68fd44ff`, `0x2afd44ff`, `0x97fd44ff`, `0x2cfd44ff`, `0x56fd44ff`, `0x20fd44ff`, `0x65fd44ff`, `0x1dfd44ff`, `0x35fd44ff`, `0x8ffd44ff`, `0x82fd44ff`, `0x798466ff`, `0x1e8466ff`, `0xab8466ff`, `0xd68466ff`, `0xc68466ff`, `0x868466ff`, `0x8d8466ff`, `0x538466ff`, `0xe48466ff`, `0xfd8466ff`, `0xce8466ff`, `0xe98466ff`, `0xd28466ff`, `0x8c8466ff`, `0xeb8466ff`, `0x6a8466ff`도 같은 high32 `0x9154852`, `x1high7=0x60..0x7f`, `x2low7={0x7e,0x7b,0x43,0x11,0x2b}` 조건에서 검사했고 각 `2621440`개가 모두 UNSAT였다. 따라서 이 제한 shard에서 쉰네 base `` 합계 `141557760`개 조합이 모두 닫혔다.
- 7번 high32 weak-filter 대표 base 확장: 상수 adder simplification 뒤 같은 high32 `0x9154852`, 대표 `x2low7={0x7e,0x7b,0x43,0x11,0x2b}`에서 `x1low32=0xd08466ff`의 `x1high7=0x00..0x1f`, `0x20..0x3f`, `0x40..0x5f` 세 shard와 `x1low32=0xc4fd44ff`의 `x1high7=0x20..0x3f`, `0x40..0x5f` 두 shard를 추가 검사했다. 모든 shard가 각각 `2621440`개 UNSAT였다. 따라서 `0xd08466ff`는 `x1high7=0x00..0x7f` 전체, `0xc4fd44ff`는 `0x20..0x7f` 전체가 닫혔고, 기존 54-base `0x60..0x7f` 제한 shard와 합산한 high32 `0x9154852` 확인 UNSAT 조합은 `154664960`개다.
- 7번 2026-06-02 추가 Go/PySAT sweep: `run_07_go_sat_filter.py`의 `--assume-x1high7-x2low7` 경로에도 `--summary-json/--summary-only`를 추가해 fixed full-`x6` 교차검사를 대량 JSONL 없이 요약할 수 있게 했다. 같은 high32 `0x9154852`, 대표 `x2low7={0x7e,0x7b,0x43,0x11,0x2b}`에서 `x1low32=0xc4fd44ff`의 남은 `x1high7=0x00..0x1f` shard를 `0x00..0x0f`, `0x10..0x17`, `0x18..0x1f`로 나누어 검사했고 추가 `2621440`개 모두 UNSAT였다. 따라서 `0xc4fd44ff`도 `x1high7=0x00..0x7f` 전체가 닫혔고, high32 `0x9154852` 제한 shard 확인 UNSAT 조합은 `157286400`개가 됐다. 대체 branch에서는 `x7=4`의 full `x6=0x033d242004b5/0x033d242004b7`, `x7=5`의 `0x017554242057/0x01755424207e/0x017554242080/0x0175542420a7`을 `x1low32=0xfb22ffff/0xd922ffff`와 교차해 각 `32768`개씩 검사했고 모두 UNSAT였다. 또한 high36 assumptions sweep으로 `x7=4,x6high=0xcf490801` 및 `x7=5,x6high=0x5d550908`, `x1low32=0x0c22ffff`, `x1high7=0x00..0x1f`, 다섯 `x2low7` 후보를 검사해 각각 `163840`개 모두 UNSAT였다. 이번 추가 제거량은 SAT `0`, UNSAT `3211264`개이며, alternative queue도 대표 base에서 계속 탈락하므로 다음 우선순위는 optimized Hensel-tail column equality 또는 더 다양한 high32/high36 prefix assumptions sweep이다.
- 7번 high32 gap 추가 확장: 같은 high32 `0x9154852`, 대표 `x2low7={0x7e,0x7b,0x43,0x11,0x2b}`에서 이전에는 `x1high7=0x60..0x7f`만 닫혀 있던 `x1low32=0x0c22ffff`와 `0xfa22ffff`를 각각 `x1high7=0x00..0x1f`까지 확장했다. 각 base는 `0x00..0x0f`, `0x10..0x1f` 두 shard로 나누어 검사했고 각각 `2621440`개 모두 UNSAT였다. 따라서 추가 제거량은 SAT `0`, UNSAT `5242880`개이며, high32 `0x9154852` 제한 shard 확인 UNSAT 조합은 `162529280`개가 됐다.
- 7번 high32 gap 두 번째 확장: 같은 조건에서 `x1low32=0xfb22ffff`, `0xd922ffff`, `0xda22ffff`, `0xfe22ffff`, `0xbc22ffff`, `0x2d22ffff`도 `x1high7=0x00..0x1f`까지 넓혔다. 여섯 base 모두 두 shard 합계 `2621440`개씩 SAT `0`, UNSAT였고, 이번 추가 제거량은 `15728640`개다. 따라서 high32 `0x9154852` 제한 shard 확인 UNSAT 조합은 `178257920`개가 됐다.
- 7번 high32 gap 중간 구간 마감 및 x2 sanity: `x1low32=0x0c22ffff`, `0xfa22ffff`의 `x1high7=0x20..0x5f`도 검사해 `10485760`개 모두 UNSAT였다. 이로써 두 base는 대표 `x2low7={0x7e,0x7b,0x43,0x11,0x2b}`에서 `x1high7=0x00..0x7f` 전체가 닫혔고, high32 `0x9154852` 제한 shard 확인 UNSAT 조합은 `188743680`개가 됐다. 별도로 `x1low32=0xfb22ffff`, `0xd922ffff`, `x1high7=0x00`에서 `x2low7=0x00..0x7f` 전체 sanity sweep을 수행했고 `4194304`개 모두 SAT `0`, UNSAT였다.
- 7번 high32 gap 추가 마감: `x1low32=0xfb22ffff`, `0xd922ffff`의 중간 gap `x1high7=0x20..0x5f`도 검사했고 `10485760`개 모두 UNSAT였다. 이로써 네 base `0x0c22ffff/0xfa22ffff/0xfb22ffff/0xd922ffff`는 대표 `x2low7` shortlist에서 `x1high7=0x00..0x7f` 전체가 닫혔고, high32 `0x9154852` 제한 shard 확인 UNSAT 조합은 `199229440`개가 됐다.
- 7번 high32 gap 추가 마감 2: 같은 middle-gap closure를 `x1low32=0xda22ffff`, `0xfe22ffff`, `0xbc22ffff`, `0x2d22ffff`에도 적용했고 `20971520`개 모두 UNSAT였다. 따라서 여덟 base `0x0c22ffff/0xfa22ffff/0xfb22ffff/0xd922ffff/0xda22ffff/0xfe22ffff/0xbc22ffff/0x2d22ffff`는 대표 `x2low7` shortlist에서 `x1high7=0x00..0x7f` 전체가 닫혔고, high32 `0x9154852` 제한 shard 확인 UNSAT 조합은 `220200960`개가 됐다.
- 7번 high32 full-x1high 추가 closure: `x1low32=0x8122ffff`, `0xde22ffff`, `0xf522ffff`, `0xa722ffff`도 `x1high7=0x00..0x5f`까지 확장했고 `31457280`개 모두 UNSAT였다. 기존 `0x60..0x7f` shard와 합쳐 이 네 base도 대표 `x2low7` shortlist에서 full `x1high7=0x00..0x7f`가 닫혔다. high32 `0x9154852` 제한 shard 확인 UNSAT 조합은 `251658240`개가 됐다.
- 7번 high32 full-x1high 추가 closure 2: `x1low32=0xf922ffff`, `0xcf22ffff`, `0xc722ffff`, `0xd422ffff`도 `x1high7=0x00..0x5f`까지 확장했고 `31457280`개 모두 UNSAT였다. 기존 `0x60..0x7f` shard와 합쳐 이 네 base도 full `x1high7=0x00..0x7f`가 닫혔고, high32 `0x9154852` 제한 shard 확인 UNSAT 조합은 `283115520`개가 됐다.
- 7번 high32 full-x1high 추가 closure 3: `x1low32=0xab22ffff`, `0x12fd44ff`, `0xdbfd44ff`, `0x5cfd44ff`도 `x1high7=0x00..0x5f`까지 확장했고 `31457280`개 모두 UNSAT였다. 기존 `0x60..0x7f` shard와 합쳐 이 네 base도 full `x1high7=0x00..0x7f`가 닫혔고, high32 `0x9154852` 제한 shard 확인 UNSAT 조합은 `314572800`개가 됐다.
- 7번 high32 full-x1high 추가 closure 4: `x1low32=0x41fd44ff`, `0x42fd44ff`, `0x60fd44ff`, `0x3bfd44ff`도 `x1high7=0x00..0x5f`까지 확장했고 `31457280`개 모두 UNSAT였다. 기존 `0x60..0x7f` shard와 합쳐 이 네 base도 full `x1high7=0x00..0x7f`가 닫혔고, high32 `0x9154852` 제한 shard 확인 UNSAT 조합은 `346030080`개가 됐다.
- 7번 high32 full-x1high 추가 closure 5: `x1low32=0x4dfd44ff`, `0x68fd44ff`, `0x2afd44ff`, `0x97fd44ff`도 `x1high7=0x00..0x5f`까지 확장했고 `31457280`개 모두 UNSAT였다. 기존 `0x60..0x7f` shard와 합쳐 이 네 base도 full `x1high7=0x00..0x7f`가 닫혔고, high32 `0x9154852` 제한 shard 확인 UNSAT 조합은 `377487360`개가 됐다.
- 7번 high32 full-x1high 추가 closure 6: `x1low32=0x2cfd44ff`, `0x56fd44ff`, `0x20fd44ff`, `0x65fd44ff`도 `x1high7=0x00..0x5f`까지 확장했고 `31457280`개 모두 UNSAT였다. 기존 `0x60..0x7f` shard와 합쳐 이 네 base도 full `x1high7=0x00..0x7f`가 닫혔고, high32 `0x9154852` 제한 shard 확인 UNSAT 조합은 `408944640`개가 됐다.
- 7번 high32 full-x1high 추가 closure 7: `x1low32=0x1dfd44ff`, `0x35fd44ff`, `0x8ffd44ff`, `0x82fd44ff`도 `x1high7=0x00..0x5f`까지 확장했고 `31457280`개 모두 UNSAT였다. 기존 `0x60..0x7f` shard와 합쳐 이 네 base도 full `x1high7=0x00..0x7f`가 닫혔고, high32 `0x9154852` 제한 shard 확인 UNSAT 조합은 `440401920`개가 됐다.
- 7번 high32 full-x1high 추가 closure 8: `x1low32=0x798466ff`, `0x1e8466ff`, `0xab8466ff`, `0xd68466ff`도 `x1high7=0x00..0x5f`까지 확장했고 `31457280`개 모두 UNSAT였다. 기존 `0x60..0x7f` shard와 합쳐 이 네 base도 full `x1high7=0x00..0x7f`가 닫혔고, high32 `0x9154852` 제한 shard 확인 UNSAT 조합은 `471859200`개가 됐다.
- 7번 high32 full-x1high 추가 closure 9: `x1low32=0xc68466ff`, `0x868466ff`, `0x8d8466ff`, `0x538466ff`도 `x1high7=0x00..0x5f`까지 확장했고 `31457280`개 모두 UNSAT였다. 기존 `0x60..0x7f` shard와 합쳐 이 네 base도 full `x1high7=0x00..0x7f`가 닫혔고, high32 `0x9154852` 제한 shard 확인 UNSAT 조합은 `503316480`개가 됐다.
- 7번 high32 full-x1high 추가 closure 10: `x1low32=0xe48466ff`, `0xfd8466ff`, `0xce8466ff`, `0xe98466ff`도 `x1high7=0x00..0x5f`까지 확장했고 `31457280`개 모두 UNSAT였다. 기존 `0x60..0x7f` shard와 합쳐 이 네 base도 full `x1high7=0x00..0x7f`가 닫혔고, high32 `0x9154852` 제한 shard 확인 UNSAT 조합은 `534773760`개가 됐다.
- 7번 high32 full-x1high 추가 closure 11: `x1low32=0xd28466ff`, `0x8c8466ff`, `0xeb8466ff`, `0x6a8466ff`도 `x1high7=0x00..0x5f`까지 확장했고 `31457280`개 모두 UNSAT였다. 기존 `0x60..0x7f` shard와 합쳐 이 네 base도 full `x1high7=0x00..0x7f`가 닫혔다. high32 `0x9154852` 제한 shard 확인 UNSAT 조합은 `566231040`개이며, 초기 54개 `x1low32` base는 대표 `x2low7` shortlist와 full `x1high7` 범위 전체에서 모두 제거됐다.
- 7번 full-x6 T-candidates runner: `run_07_go_sat_filter.py`의 full-`x6` `--assume-x1high7-x2low7` 경로에 `--T-candidates 784,800,848`을 추가했다. `x6=0x24552149094,x1low32=0xc4fd44ff,x1high7=0x61,x2low7=0x7e` smoke에서 세 T 모두 UNSAT였고, `x1low32=0x2cfd44ff,x1high7=0x00..0x5f,대표 x2low7 5개` 진단도 T별 `480`개씩 모두 UNSAT였다. `T=784/800/848`의 CNF 크기는 각각 `2664/6085`, `2697/6117`, `2796/6213` vars/clauses였으므로, full-`x6` shortlist 검증은 작은 `T=784`부터 시작하는 쪽이 낫다. 단 같은 후보를 T별로 반복 검사한 값이라 high32 bucket 제거 누계에는 더하지 않는다.
- 7번 추가 CP-SAT 장기 probe: 같은 대표 cube를 `T=784,tail_lims=79,compact-q-limbs,decision-q-range 704:64,skip-known-prefix-limbs 17`로 45초 제한 실행했다. 실제 wall time은 `68.98s`, status `UNKNOWN`, conflicts `12234`, branches `15301`, final carry zero enforced였다. short sweep에서 activity가 높아도 full-tail은 아직 닫히지 않으므로, 단일 CP-SAT 장기 실행보다 SAT/CNF 병렬 cube가 계속 우선이다.
- 7번 high32 next-base sweep: high32 `0x9154852` bucket에서 초기 54개 `x1low32` base를 full `x1high7` 범위까지 닫은 뒤, 아직 닫히지 않은 `8466ff` 계열 다음 16개 base `0x218466ff/0x1c8466ff/0xa78466ff/0x718466ff/0xa58466ff/0x998466ff/0xcc8466ff/0xe88466ff/0xe38466ff/0x188466ff/0x788466ff/0xc48466ff/0x928466ff/0x808466ff/0x828466ff/0x858466ff`도 `x1high7=0x60..0x7f`, 대표 `x2low7={0x7e,0x7b,0x43,0x11,0x2b}`, `x6low14` 전체 조건에서 Go/PySAT `arith_bits=272 + q_interval_bound`로 검사했다. 추가 `41,943,040`개 모두 SAT `0`, UNSAT였고, high32 제한 shard 확인량은 `608,174,080`개가 됐다. 이 16개는 아직 `x1high7=0x00..0x5f`가 닫힌 것은 아니므로 full closure 누계와 구분한다.
- 7번 high32 next-base full closure: 위 16개 중 다음 네 base `0x218466ff/0x1c8466ff/0xa78466ff/0x718466ff`를 `x1high7=0x00..0x5f`까지 추가 확장했고, `31,457,280`개 모두 SAT `0`, UNSAT였다. 기존 `0x60..0x7f` shard와 합치면 이 네 base도 full `x1high7=0x00..0x7f`가 닫혔다. 따라서 high32 `0x9154852` 제한 shard 확인량은 `639,631,360`개가 됐고, full closure base는 초기 54개에서 58개로 늘었다.
- 7번 high32 next-base full closure 2: 이어 `0xa58466ff/0x998466ff/0xcc8466ff/0xe88466ff`도 `x1high7=0x00..0x5f`까지 확장했고, 추가 `31,457,280`개 모두 SAT `0`, UNSAT였다. 기존 `0x60..0x7f` shard와 합치면 이 네 base도 full `x1high7=0x00..0x7f`가 닫혔다. high32 `0x9154852` 제한 shard 확인량은 `671,088,640`개, full closure base는 62개가 됐다.
- 7번 high32 next-base full closure 3: 남은 next-base `0xe38466ff/0x188466ff/0x788466ff/0xc48466ff/0x928466ff/0x808466ff/0x828466ff/0x858466ff`도 `x1high7=0x00..0x5f`까지 확장했고, 추가 `62,914,560`개 모두 SAT `0`, UNSAT였다. 기존 `0x60..0x7f` shard와 합치면 이 8개도 full `x1high7=0x00..0x7f`가 닫혔다. 따라서 high32 `0x9154852` 제한 shard 확인량은 `734,003,200`개가 됐고, 초기 54개와 next-base 16개를 합친 총 70개 `x1low32` base가 대표 `x2low7` shortlist에서 full closure 상태가 됐다.
- 7번 high32 more-8466 full closure: 아직 문서화되지 않았던 `8466ff` 계열 8개 base `0x008466ff/0x118466ff/0x228466ff/0x338466ff/0x448466ff/0x558466ff/0x668466ff/0x778466ff`도 같은 high32 `0x9154852`, 대표 `x2low7={0x7e,0x7b,0x43,0x11,0x2b}`, `x6low14` 전체 조건에서 검사했다. `x1high7=0x60..0x7f` shard는 `20,971,520`개, `0x00..0x5f` shard는 `62,914,560`개 모두 SAT `0`, UNSAT였고, 이 8개도 full `x1high7=0x00..0x7f`가 닫혔다. 따라서 high32 `0x9154852` 제한 shard 확인량은 `817,889,280`개, full closure base는 총 78개로 늘었다.
- 7번 Go tail-window CNF: `go_hensel_tail`에 `tail_window_bits` / `--tail-window-bits`를 추가했다. `T` 지점의 lower carry를 정확히 계산하지는 않고 12비트 자유 carry-in으로 두는 보수적 window equality라 full optimized tail-lock은 아니지만, `arith_bits=T+window`보다 훨씬 작게 `T` 이후 product bit를 보강한다. 대표 `T=800`, high32 `x6=0x9154852`, `x1low32=0xd08466ff`, `x1high7=0x60`, `x2low7=0x7e`, `arith_bits=272`, `q_interval_bound=True`에서 `tail_window_bits=16`은 `24,984` vars / `105,144` clauses로 단일 tuple 및 `x6low14` 전체 `16,384`개를 모두 UNSAT로 닫았다. `tail_window_bits=32` 단일 tuple은 `48,497` vars / `209,998` clauses로 UNSAT였다. `arith_bits=0` tail-window-only broad smoke는 1분 이상 걸려 중단했으므로, 단독 prefilter보다 weak prefix에 붙이는 shortlist/bucket verifier로 둔다.
- 7번 tail-window full-x2 sanity: 같은 `tail_window_bits=16 + arith_bits=272` 결합 필터로 `x1low32=0x008466ff`, high32 `x6=0x9154852`, `x6low14` 전체에서 `x1high7=0x00/0x20/0x60` 세 지점을 각각 `x2low7=0x00..0x7f` 전체로 확장했다. 각 sweep은 `2,097,152`개 모두 SAT `0`, UNSAT였고 합계 `6,291,456`개가 닫혔다. 이는 대표 `x2low7` shortlist 밖에서도 해당 base의 낮은/중간/높은 high shard가 모순임을 확인하는 sanity check다.
- 7번 tail-window full-x2 추가 sanity: 같은 high32 `0x9154852`와 `x1high7=0x60` 지점에서 more-8466 묶음 나머지 일곱 base `0x118466ff/0x228466ff/0x338466ff/0x448466ff/0x558466ff/0x668466ff/0x778466ff`도 `x2low7=0..127`, `x6low14=0..16383` 전체가 모두 UNSAT였다. 추가 확인량은 `14,680,064`개이고, 기존 `0x008466ff`의 같은 high shard까지 합치면 8개 base에서 `16,777,216`개 조합이 닫혔다. 이 결과는 `x2low7` shortlist 밖 sanity를 넓히지만, 아직 전체 `x1high7` 128개에 대한 full-x2 증명은 아니다.
- 7번 tail-window three-shard sanity: 위 일곱 base의 `x1high7=0x00` 및 `0x20`도 같은 full-`x2low7` 방식으로 추가 검사했고, 새 `14`개 sweep의 `29,360,128`개 조합도 모두 SAT `0`, UNSAT였다. 따라서 more-8466 8개 base 전체는 high32 `0x9154852`, `x6low14` 전체에서 `x1high7=0x00/0x20/0x60` 세 대표 지점과 `x2low7=0..127` 전체가 닫혔고, 세 지점 총계는 `50,331,648`개다. 아직 전체 `x1high7` 128개 full closure는 아니지만, 대표 `x2low7` shortlist 밖 sanity가 넓어졌다.
- 7번 tail-window four-shard sanity: 같은 more-8466 8개 base에 `x1high7=0x40` 지점도 추가했고, `8`개 sweep의 `16,777,216`개 조합도 모두 SAT `0`, UNSAT였다. 따라서 `x1high7=0x00/0x20/0x40/0x60` 네 대표 지점 합계는 `67,108,864`개 모두 UNSAT다. 이는 여전히 full `x1high7` 128개 closure는 아니지만, 같은 high32 bucket이 대표 `x2low7` shortlist 밖에서도 넓게 탈락한다는 증거다.
- 7번 tail-window six-shard sanity: 같은 more-8466 8개 base에 사이 지점 `x1high7=0x10/0x30`도 추가했고, 새 `16`개 sweep의 `33,554,432`개 조합도 모두 SAT `0`, UNSAT였다. 이제 `x1high7=0x00/0x10/0x20/0x30/0x40/0x60` 여섯 대표 지점의 full-`x2low7` sanity 총계는 `100,663,296`개다. 아직 `0x50/0x70` 및 내부 high 값은 남아 있으므로 full closure는 아니지만, high32 `0x9154852` bucket의 우선순위는 더 낮아졌다.
- 7번 tail-window eight-shard sanity: 남아 있던 대표 지점 `x1high7=0x50/0x70`도 같은 more-8466 8개 base에서 full-`x2low7` 방식으로 추가 검사했고, 새 `16`개 sweep의 `33,554,432`개 조합도 모두 SAT `0`, UNSAT였다. 이제 `x1high7=0x00/0x10/0x20/0x30/0x40/0x50/0x60/0x70` 여덟 대표 지점의 full-`x2low7` sanity 총계는 `134,217,728`개다. 전체 high 7비트 closure는 아니지만, high32 `0x9154852` bucket은 대표 high grid에서도 계속 탈락한다.
- 7번 Go exact-tail 구현 주의: 현재 exporter의 column/carry metadata와 `tail_window` free carry는 실제 lower Hensel carry와 연결되어 있지 않다. exact-tail equality는 `carry_nonzero_marker_*`나 free carry를 재사용하지 말고, lower recurrence에서 나온 multi-bit `carry_T`를 CNF 내부에서 계산해 tail limb column으로 넘기는 별도 bit-vector network로 구현해야 한다. 검증 전 broad UNSAT proof에 쓰면 false UNSAT 위험이 있다.
- 7번 tail-window/oracle 비교 도구: `compare_07_tail_oracle.py`를 추가해 같은 assumption tuple을 weak `tail_window` 필터와 product-prefix oracle로 각각 풀 수 있게 했다. 대표 `T=800`, high32 `x6=0x9154852`, `x1low32=0x008466ff`, `x1high7=0x50`, `x2low7=0x00`, `x6low=0`, `q_interval_bound=True`에서는 weak `arith_bits=272 + tail_window_bits=16`이 `24799` vars / `104355` clauses로 UNSAT, oracle `arith_bits=816`이 `424158` vars / `1901622` clauses로 UNSAT였고 verdict는 `both_unsat`이다.
- 7번 tail-window/oracle batch: 같은 high32 `0x9154852`, `T=800`, `q_interval_bound=True` 조건에서 4개 샘플을 더 비교했고 모두 verdict `both_unsat`이었다. weak CNF는 `24799..25082` vars / `104355..105648` clauses, product-prefix oracle은 `424158..428140` vars / `1901622..1920240` clauses였다. 이 bucket은 oracle 샘플에서도 계속 모순이므로 더 깊은 반복보다 exact-tail carry-vector 구현이나 diversity bucket 쪽이 낫다.
- 7번 alternative branch product-prefix oracle: `x7=4/5`의 alternative full-`x6` 후보 4개 tuple을 `T=784`, `arith_bits=800`, `q_interval_bound=True`로 spot-check했고 모두 SAT `0`, UNSAT였다. CNF 크기는 `407036..407254` vars / `1826354..1827595` clauses였다. alternative queue도 대표 tuple에서는 강한 product-prefix 조건을 통과하지 못한다.
- 7번 tail-window carry sweep: `compare_07_tail_oracle.py`에 `--tail-window-carry-bits-list`를 추가했다. 대표 high32 `0x9154852`, `T=800`, `x1low32=0x008466ff`, `x1high7=0x70`, `x2low7=0x7e`, `x6low=0`에서 carry bits `8,10,12,14,16`을 비교했고 모두 product-prefix oracle과 같은 `both_unsat`이었다. weak CNF는 `24794..24814` vars / `104341..104390` clauses, oracle은 `424158` vars / `1901622` clauses였다.
- 7번 diversity branch product-prefix oracle: 넓은 diversity sweep에서 남아 있던 `x7=1/2/3/6` full-`x6` 후보 4개 tuple도 `T=784`, `arith_bits=800`, `q_interval_bound=True`로 spot-check했고 모두 SAT `0`, UNSAT였다. CNF 크기는 `402561..405642` vars / `1805419..1819624` clauses였다. 기존 high32/alternative 대표 tuple과 함께 q-prefix/tail848 ranking queue가 강한 product-prefix 조건에서는 계속 탈락하므로, 다음 구현 우선순위는 true lower carry-vector를 갖는 Hensel-tail CNF와 SAT cube-and-conquer다.
- 7번 product-prefix runner 편의 옵션: `run_07_go_sat_filter.py`에 `--exact-tail-limbs`를 추가했다. `--T 784 --exact-tail-limbs 1`은 `arith_bits=800`과 같은 안전한 product-prefix oracle이고, 대표 diversity tuple smoke에서 같은 `405636` vars / `1819624` clauses, SAT `0`, UNSAT 결과를 냈다. 여러 `T-candidates`를 훑을 때 weak `tail_window`와 product-prefix oracle을 혼동하지 않기 위한 옵션이다.
- 7번 exact-tail T sweep: active full-`x6` shortlist의 두 tuple을 `T-candidates=784,800,848`, `exact_tail_limbs=1`, `q_interval_bound=True`로 비교했고 둘 다 세 T 모두 UNSAT였다. `x6=0x245521490bd,x1low32=0xd08466ff`는 `T=784/800/848`에서 `402576/425050/493007` vars, `x6=0x24552149098,x1low32=0xfe22ffff`는 `408419/431298/499979` vars였다. 같은 결론이면 `T=784`가 가장 작으므로 full-`x6` product-prefix verifier는 낮은 T부터 적용한다.
- 7번 x6low T-candidates 지원: `run_07_go_sat_filter.py`의 `--assume-x6low-x1high7-x2low7` 모드도 `--T-candidates`를 받을 수 있게 했다. high44 `x6high=0x9154852425`, `x6low=0..3`, `x1low32=0xc4fd44ff`, `x1high7=0x61`, `x2low7=0x7e`, `exact_tail_limbs=1` smoke에서 `T=784`는 boundary `786` 때문에 skip됐고, `T=800`은 `429322` vars / `1925098` clauses로 네 assumptions 모두 UNSAT였다. full `x6`를 확정하기 전 prefix sweep에도 safe product-prefix oracle을 붙일 수 있다.
- 7번 high40 exact-tail x6low sweep: 주요 후보가 공유하는 `x6high_bits=40`, `x6high=0x915485242` 아래에서 `x6low=0..63`, `x1low32=0xc4fd44ff`, `x1high7=0x61`, `x2low7=0x7e`, `exact_tail_limbs=1`을 실행했다. `T=784`는 boundary `790` 때문에 skip됐고, `T=800`은 `429611` vars / `1926508` clauses로 low6 64개 모두 UNSAT였다. 특정 tuple sanity지만 high40 prefix도 safe product-prefix oracle에서 처리 가능함을 확인했다.
- 7번 high40 exact-tail full x1high sweep: 같은 high40 `x6high=0x915485242`, `x1low32=0xc4fd44ff`, `x2low7=0x7e`에서 `x1high7=0x00..0x7f`과 `x6low=0..63`을 함께 assumptions로 훑었다. `T=784`는 boundary `790` 때문에 skip됐고, `T=800` product-prefix oracle은 `429611` vars / `1926508` clauses, assumptions `20`개로 전체 `8192`개 모두 SAT `0`, UNSAT였다. 특정 `x1low32/x2low7` sanity이지만, 이 high40 queue는 safe oracle 기준에서 full `x1high7` 범위가 닫혔다. Python은 runner/orchestration에 두고, 이처럼 큰 반복 CNF 생성은 Go exporter로 처리하는 구조가 더 적합하다.
- 7번 high40 exact-tail x2 shortlist sweep: 같은 high40 `x6high=0x915485242`, `x1low32=0xc4fd44ff`에서 대표 `x2low7={0x7e,0x7b,0x43,0x11,0x2b}` 전체를 `x1high7=0x00..0x7f`, `x6low=0..63`와 교차해 검사했다. `T=784`는 boundary `790` 때문에 skip됐고, `T=800` product-prefix oracle은 같은 `429611` vars / `1926508` clauses로 각 x2 값당 `8192`개, 전체 `40960`개 모두 SAT `0`, UNSAT였다. 이 결과도 특정 base/prefix sanity지만, `0xc4fd44ff` base와 해당 high40 bucket은 대표 x2 shortlist에서 우선순위를 낮춘다.
- 7번 high40 exact-tail 추가 base sweep: 같은 high40 `x6high=0x915485242`에서 `x1low32=0x0c22ffff`와 `0xfa22ffff`도 대표 `x2low7={0x7e,0x7b,0x43,0x11,0x2b}`, `x1high7=0x00..0x7f`, `x6low=0..63` 전체로 검사했다. 두 base 모두 `T=784`는 boundary `790` 때문에 skip됐고, `T=800` product-prefix oracle에서 각 `40960`개 모두 SAT `0`, UNSAT였다. CNF 크기는 각각 `427859/1918858`, `431525/1935789` vars/clauses다. main high40 bucket의 대표 x1low base들이 safe oracle에서도 계속 탈락한다.
- 7번 high40 exact-tail 추가 base sweep 2: 같은 조건으로 `x1low32=0xfb22ffff`와 `0xd922ffff`도 검사했다. 두 base 모두 대표 `x2low7` 다섯 값, full `x1high7`, full low6 아래에서 각 `40960`개 모두 SAT `0`, UNSAT였고, CNF 크기는 `431442/1935523`, `431160/1933962` vars/clauses다. high40 `0x915485242` 아래 safe oracle에서 닫힌 대표 base는 이제 `0xc4fd44ff`, `0x0c22ffff`, `0xfa22ffff`, `0xfb22ffff`, `0xd922ffff`까지 늘었다.
- 7번 high40 exact-tail 추가 base sweep 3: 같은 high40 `x6high=0x915485242`에서 `x1low32=0xda22ffff`, `0xfe22ffff`, `0xbc22ffff`, `0x2d22ffff`도 대표 `x2low7` 다섯 값, full `x1high7`, full low6 아래에서 검사했다. 네 base 모두 `T=784`는 boundary `790` 때문에 skip됐고, `T=800` product-prefix oracle에서 각 `40960`개씩 총 `163840`개 모두 SAT `0`, UNSAT였다. CNF 크기는 `430641/1931547`, `432599/1940753`, `430359/1930554`, `428797/1923325` vars/clauses다. high40 `0x915485242` 아래 safe oracle에서 닫힌 대표 base는 총 9개로 늘었지만, 아직 high40 bucket 전체 증명은 아니다.
- 7번 high40 exact-tail 추가 base sweep 4: 같은 조건으로 `x1low32=0x8122ffff`, `0xde22ffff`, `0xf522ffff`, `0xa722ffff`도 검사했다. 네 base 모두 `T=784`는 boundary `790` 때문에 skip됐고, `T=800` product-prefix oracle에서 각 `40960`개씩 총 `163840`개 모두 SAT `0`, UNSAT였다. CNF 크기는 `427580/1917267`, `431749/1936623`, `431162/1933921`, `430089/1928986` vars/clauses다. high40 `0x915485242` 아래 safe oracle에서 닫힌 대표 `x1low32` base는 총 13개로 늘었다.
- 7번 high40 exact-tail 추가 base sweep 5: 같은 조건으로 `x1low32=0xf922ffff`, `0xcf22ffff`, `0xc722ffff`, `0xd422ffff`도 검사했다. 네 base 모두 `T=784`는 boundary `790` 때문에 skip됐고, `T=800` product-prefix oracle에서 각 `40960`개씩 총 `163840`개 모두 SAT `0`, UNSAT였다. CNF 크기는 `431730/1936566`, `430680/1931660`, `429680/1926998`, `428436/1921454` vars/clauses다. high40 `0x915485242` 아래 safe oracle에서 닫힌 대표 `x1low32` base는 총 17개로 늘었다.
- 7번 high40 exact-tail 추가 base sweep 6: 같은 조건으로 `x1low32=0xab22ffff`, `0x12fd44ff`, `0xdbfd44ff`, `0x5cfd44ff`도 검사했다. 네 base 모두 `T=784`는 boundary `790` 때문에 skip됐고, `T=800` product-prefix oracle에서 각 `40960`개씩 총 `163840`개 모두 SAT `0`, UNSAT였다. CNF 크기는 `429993/1928712`, `428385/1921077`, `429698/1927425`, `428795/1923129` vars/clauses다. high40 `0x915485242` 아래 safe oracle에서 닫힌 대표 `x1low32` base는 총 21개로 늘었다.
- 7번 high40 exact-tail 추가 base sweep 7: 같은 조건으로 `x1low32=0x41fd44ff`, `0x42fd44ff`, `0x60fd44ff`, `0x3bfd44ff`, `0x4dfd44ff`, `0x68fd44ff`, `0x2afd44ff`, `0x97fd44ff`도 검사했다. 여덟 base 모두 `T=784`는 boundary `790` 때문에 skip됐고, `T=800` product-prefix oracle에서 각 `40960`개씩 총 `327680`개 모두 SAT `0`, UNSAT였다. CNF 크기는 `427970/1919075`, `428415/1921203`, `428531/1921595`, `429723/1927560`, `428298/1920770`, `429015/1923854`, `428894/1923478`, `430495/1930798` vars/clauses다. high40 `0x915485242` 아래 safe oracle에서 닫힌 대표 `x1low32` base는 총 29개로 늘었다.
- 7번 high40 exact-tail 추가 base sweep 8: 같은 조건으로 `x1low32=0x2cfd44ff`, `0x56fd44ff`, `0x20fd44ff`, `0x65fd44ff`, `0x1dfd44ff`, `0x35fd44ff`, `0x8ffd44ff`, `0x82fd44ff`도 검사했다. 여덟 base 모두 `T=784`는 boundary `790` 때문에 skip됐고, `T=800` product-prefix oracle에서 각 `40960`개씩 총 `327680`개 모두 SAT `0`, UNSAT였다. CNF 크기는 `427645/1917879`, `429289/1925446`, `425848/1909842`, `428688/1922695`, `427587/1917704`, `428615/1922471`, `431099/1933542`, `428416/1921216` vars/clauses다. high40 `0x915485242` 아래 safe oracle에서 닫힌 대표 `x1low32` base는 총 37개로 늘었다.
- 7번 high40 exact-tail 추가 base sweep 9: 같은 조건으로 `x1low32=0x798466ff`, `0x1e8466ff`, `0xab8466ff`, `0xd68466ff`, `0xc68466ff`, `0x868466ff`, `0x8d8466ff`, `0x538466ff`도 검사했다. 여덟 base 모두 `T=784`는 boundary `790` 때문에 skip됐고, `T=800` product-prefix oracle에서 각 `40960`개씩 총 `327680`개 모두 SAT `0`, UNSAT였다. CNF 크기는 `427967/1918982`, `427244/1915933`, `427322/1916147`, `428607/1921818`, `428116/1919508`, `425706/1908721`, `426862/1913985`, `426494/1912075` vars/clauses다. high40 `0x915485242` 아래 safe oracle에서 닫힌 대표 `x1low32` base는 총 45개로 늘었다.
- 7번 high40 exact-tail 추가 base sweep 10: 같은 조건으로 `x1low32=0xe48466ff`, `0xfd8466ff`, `0xce8466ff`, `0xe98466ff`, `0xd28466ff`, `0x8c8466ff`, `0xeb8466ff`, `0x6a8466ff`도 검사했다. 여덟 base 모두 `T=784`는 boundary `790` 때문에 skip됐고, `T=800` product-prefix oracle에서 각 `40960`개씩 총 `327680`개 모두 SAT `0`, UNSAT였다. CNF 크기는 `426774/1913628`, `431297/1934229`, `429135/1924310`, `428036/1919213`, `426898/1914096`, `426956/1914300`, `427774/1918331`, `426890/1914033` vars/clauses다. high40 `0x915485242` 아래 safe oracle에서 닫힌 대표 `x1low32` base는 총 53개로 늘었다.
- 7번 high40 exact-tail 대표 base closure: 앞서 별도 high40 multi-x2 sweep으로 닫았던 `x1low32=0xd08466ff`도 같은 `T-candidates=784,800`, `exact_tail_limbs=1`, `q_interval_bound=True` 형식으로 재검사했다. `T=784`는 boundary `790` 때문에 skip됐고, `T=800` product-prefix oracle에서 `40960`개 모두 SAT `0`, UNSAT였으며 CNF 크기는 `426437/1911857` vars/clauses다. 따라서 high32 `0x9154852`에서 추적하던 초기 54개 대표 `x1low32` base는 high40 `x6high=0x915485242` prefix와 safe product-prefix oracle 기준에서도 모두 닫혔다. 단 이는 전체 high40 bucket proof가 아니라 대표 base closure다.
- 7번 high40 exact-tail next-base closure: 같은 high40 `x6high=0x915485242`와 safe product-prefix oracle 조건으로 next-base 16개 `0x218466ff/0x1c8466ff/0xa78466ff/0x718466ff/0xa58466ff/0x998466ff/0xcc8466ff/0xe88466ff/0xe38466ff/0x188466ff/0x788466ff/0xc48466ff/0x928466ff/0x808466ff/0x828466ff/0x858466ff`도 검사했다. `x6low=0..63`, `x1high7=0x00..0x7f`, 대표 `x2low7={0x7e,0x7b,0x43,0x11,0x2b}`의 총 `655360`개가 모두 SAT `0`, UNSAT였고, `T=784`는 boundary `790` 때문에 모두 skip, `T=800`만 사용됐다. CNF 크기는 `424308..428311` vars / `1902688..1920761` clauses 범위다. high40 safe oracle 기준 대표 closure base는 54개에서 70개로 늘었다.
- 7번 high40 exact-tail more-8466 closure: 이어 more-8466 8개 `0x008466ff/0x118466ff/0x228466ff/0x338466ff/0x448466ff/0x558466ff/0x668466ff/0x778466ff`도 같은 조건으로 검사했고 총 `327680`개 모두 SAT `0`, UNSAT였다. CNF 크기는 `423637..427633` vars / `1899147..1917849` clauses 범위다. 따라서 high40 `x6high=0x915485242` prefix와 safe product-prefix oracle 기준으로 닫힌 대표 `x1low32` base는 초기 54개, next-base 16개, more-8466 8개를 합쳐 총 78개가 됐다. 총 `3194880`개 assumptions가 모두 UNSAT였지만, 여전히 전체 high40 bucket proof가 아니라 conflict-ranking/추가 대표 base closure다.
- 7번 high36 exact-tail representative sweep: high40보다 더 넓은 `x6high_bits=36`, `x6high=0x91548524` 아래에서 대표 `x1low32` 10개 `0xc4fd44ff/0xd08466ff/0x0c22ffff/0xfa22ffff/0xfb22ffff/0xd922ffff/0xda22ffff/0xfe22ffff/0xbc22ffff/0x2d22ffff`를 검사했다. `x6low=0..1023`, `x1high7=0x00..0x7f`, 대표 `x2low7={0x7e,0x7b,0x43,0x11,0x2b}`, `T-candidates=784,800`, `exact_tail_limbs=1`, `q_interval_bound=True` 조건이다. `T=784`는 boundary `794` 때문에 모두 skip됐고, `T=800` product-prefix oracle에서 총 `6553600`개 assumptions가 모두 SAT `0`, UNSAT였다. CNF 크기는 `426663..432843` vars / `1912987..1941954` clauses 범위다. 이는 high36 prefix 전체 proof는 아니지만, high40 `0x915485242`보다 넓은 `x6` 후보 1024개 폭에서도 대표 base들이 모순임을 보여 준다.
- 7번 high36 exact-tail 추가 representative sweep: 같은 high36 `x6high=0x91548524`에서 추가 `x1low32` 8개 `0x8122ffff/0xde22ffff/0xf522ffff/0xa722ffff/0xf922ffff/0xcf22ffff/0xc722ffff/0xd422ffff`도 full `x6low=0..1023`, full `x1high7`, 대표 `x2low7` 다섯 값으로 검사했다. `T=784`는 boundary `794` 때문에 skip됐고, `T=800`, `exact_tail_limbs=1`, `q_interval_bound=True` product-prefix oracle에서 총 `5242880`개 assumptions가 모두 SAT `0`, UNSAT였다. CNF 크기는 `427861..432018` vars / `1918608..1937915` clauses 범위다. high36 safe oracle 기준으로 닫힌 대표 `x1low32` base는 이제 18개, 총 `11796480`개 assumptions다.
- 7번 high36 exact-tail 추가 representative sweep 2: 같은 high36 prefix에서 `0xab22ffff/0x12fd44ff/0xdbfd44ff/0x5cfd44ff/0x41fd44ff/0x42fd44ff/0x60fd44ff/0x3bfd44ff`도 같은 조건으로 검사했다. 다시 `T=784`는 boundary `794` 때문에 skip됐고, `T=800` product-prefix oracle에서 총 `5242880`개 assumptions가 모두 SAT `0`, UNSAT였다. CNF 크기는 `428239..430239` vars / `1920386..1929934` clauses 범위다. high36 safe oracle 기준 대표 closure는 26개 base, 총 `17039360`개 assumptions로 늘었다.
- 7번 high36 exact-tail 추가 representative sweep 3: 같은 high36 prefix에서 high40 sweep 7/8의 남은 대표 base `0x4dfd44ff/0x68fd44ff/0x2afd44ff/0x97fd44ff/0x2cfd44ff/0x56fd44ff/0x20fd44ff/0x65fd44ff`도 검사했다. 다시 `T=784`는 boundary `794` 때문에 skip됐고, `T=800` product-prefix oracle에서 총 `5242880`개 assumptions가 모두 SAT `0`, UNSAT였다. CNF 크기는 `426103..430765` vars / `1911069..1932097` clauses 범위다. high36 safe oracle 기준 representative closure는 34개 base, 총 `22282240`개 assumptions로 늘었다.
- 7번 high36 exact-tail 추가 representative sweep 4: 같은 high36 prefix에서 `0x1dfd44ff/0x35fd44ff/0x8ffd44ff/0x82fd44ff/0x798466ff/0x1e8466ff/0xab8466ff/0xd68466ff`도 검사했다. 다시 `T=784`는 boundary `794` 때문에 skip됐고, `T=800` product-prefix oracle에서 총 `5242880`개 assumptions가 모두 SAT `0`, UNSAT였다. CNF 크기는 `427508..431345` vars / `1917201..1934736` clauses 범위다. high36 safe oracle 기준 representative closure는 42개 base, 총 `27525120`개 assumptions로 늘었다.
- 7번 odd-residue smoke: high36 대표 tuple `x6high=0x91548524`, `x6low=0x094`, `x1low32=0xd08466ff`, `x1high7=0x60`, `x2low7=0x7e`에 `--odd-residue-prime 3/5/7`을 추가해 Go exporter 경로를 검증했다. `T=784`는 boundary `794`로 skip됐고, `T=800`, `exact_tail_limbs=1`, `q_interval_bound=True`에서 SAT `0`, UNSAT `1`이었다. CNF 크기는 `457413` vars / `2032963` clauses로, 동일 tuple의 odd-residue 없는 high36 sweep 기록 `426663` vars / `1912987` clauses보다 커진다. product-prefix로 이미 UNSAT인 tuple이므로 새 후보 제거 신호가 아니라, weak/tail-window 모델에 붙일 보조 제약 smoke로만 해석한다.
- 7번 high36 exact-tail 추가 representative sweep 5: 같은 high36 prefix에서 남은 초기 high40 대표 일부 `0xc68466ff/0x868466ff/0x8d8466ff/0x538466ff/0xe48466ff/0xfd8466ff/0xce8466ff/0xe98466ff`도 검사했다. 다시 `T=784`는 boundary `794` 때문에 skip됐고, `T=800` product-prefix oracle에서 총 `5242880`개 assumptions가 모두 SAT `0`, UNSAT였다. CNF 크기는 `425975..431536` vars / `1910012..1935415` clauses 범위다. high36 safe oracle 기준 representative closure는 50개 base, 총 `32768000`개 assumptions로 늘었다.
- 7번 high36 초기 54-base representative closure: 마지막 네 base `0xd28466ff/0x8c8466ff/0xeb8466ff/0x6a8466ff`도 같은 high36 `x6high=0x91548524`, full `x6low`, full `x1high7`, 대표 `x2low7` 다섯 값으로 검사했다. `T=784`는 boundary `794` 때문에 skip됐고, `T=800` product-prefix oracle에서 총 `2621440`개 assumptions가 모두 SAT `0`, UNSAT였다. CNF 크기는 `427151..428047` vars / `1915339..1919630` clauses 범위다. 이로써 high40/high32에서 추적하던 초기 54개 representative `x1low32` base가 high36 prefix에서도 모두 닫혔고, 누계는 `35389440`개 assumptions다. 이는 여전히 전체 high36 bucket proof가 아니라 representative-set closure다.
- 7번 high36 tail-window full-x2 sanity: next-base `x1low32=0x218466ff`를 high36 `x6high=0x91548524` 아래에서 `x1high7={0x00,0x20,0x40,0x60}`, `x2low7=0..127`, `x6low=0..1023`로 검사했다. 약한 필터는 `T=800`, `arith_bits=272`, `tail_window_bits=16`, `tail_window_carry_bits=16`, `q_interval_bound=True`, `odd_residue_primes={3,5,7}`이며 `524288`개 assumptions가 모두 SAT `0`, UNSAT였다. CNF 크기는 `55405` vars / `223698` clauses다. 단일 tuple `(x1high7=0,x2low7=0,x6low=0)`을 `exact_tail_limbs=1` product-prefix oracle로 교차 확인했고, 이 oracle도 `424582` vars / `1903999` clauses에서 SAT `0`, UNSAT였다. 이 결과는 대표 `x2low7` shortlist 밖 sanity를 넓힌 것이며, weak filter 전체가 product-prefix oracle과 항상 동치라는 증명은 아니다.
- 7번 high36 tail-window full-x2 sanity 2: 다음 next-base `x1low32=0x1c8466ff`에도 같은 조건을 적용했다. `x1high7={0x00,0x20,0x40,0x60}`, `x2low7=0..127`, `x6low=0..1023`, `T=800`, `arith_bits=272`, `tail_window_bits=16`, `tail_window_carry_bits=16`, `q_interval_bound=True`, odd residues `3/5/7`에서 `524288`개 assumptions가 모두 SAT `0`, UNSAT였다. CNF 크기는 `55502` vars / `224055` clauses다. 단일 tuple product-prefix crosscheck도 `T=784`는 boundary `794`로 skip, `T=800`, `exact_tail_limbs=1`, `q_interval_bound=True`에서 `427691` vars / `1917845` clauses, SAT `0`, UNSAT `1`이었다.
- 7번 high36 tail-window full-x2 sanity 3: 세 번째 next-base `x1low32=0xa78466ff`도 같은 조건에서 `524288`개 assumptions가 모두 SAT `0`, UNSAT였다. weak CNF 크기는 `55609` vars / `224539` clauses다. 단일 tuple product-prefix crosscheck는 `T=784` boundary `794` skip, `T=800`, `exact_tail_limbs=1`, `q_interval_bound=True`에서 `428632` vars / `1922270` clauses, SAT `0`, UNSAT `1`이었다. 현재 weak full-`x2low7` sanity가 닫힌 high36 next-base는 `0x218466ff/0x1c8466ff/0xa78466ff` 세 개다.
- 7번 high36 tail-window full-x2 sanity 4: 네 번째 next-base `x1low32=0x718466ff`도 같은 조건에서 `524288`개 assumptions가 모두 SAT `0`, UNSAT였다. weak CNF 크기는 `55582` vars / `224412` clauses다. 단일 tuple product-prefix crosscheck는 `T=784` boundary `794` skip, `T=800`, `exact_tail_limbs=1`, `q_interval_bound=True`에서 `427826` vars / `1918265` clauses, SAT `0`, UNSAT `1`이었다. 현재 weak full-`x2low7` sanity가 닫힌 high36 next-base는 `0x218466ff/0x1c8466ff/0xa78466ff/0x718466ff` 네 개이며 weak-filter assumption 누계는 `2097152`개다.
- 7번 high36 tail-window full-x2 sanity 5/6: 다음 next-base `x1low32=0xa58466ff`와 `0x998466ff`도 같은 조건에서 base별 `524288`개 assumptions가 모두 SAT `0`, UNSAT였다. weak CNF 크기는 각각 `55530` vars / `224223` clauses, `55568` vars / `224363` clauses다. 단일 tuple product-prefix crosscheck도 각각 `T=784` boundary `794` skip, `T=800`, `exact_tail_limbs=1`, `q_interval_bound=True`에서 `426913` vars / `1914485` clauses와 `426732` vars / `1913281` clauses로 SAT `0`, UNSAT `1`이었다. 현재 weak full-`x2low7` sanity가 닫힌 high36 next-base는 여섯 개이며 weak-filter assumption 누계는 `3145728`개다.
- 7번 high36 tail-window full-x2 sanity 7-10: 이어 `x1low32=0xcc8466ff/0xe88466ff/0xe38466ff/0x188466ff`도 같은 조건에서 base별 `524288`개 assumptions가 모두 SAT `0`, UNSAT였다. weak CNF 크기는 각각 `55562/224342`, `55575/224370`, `55618/224580`, `55446/223825` vars/clauses다. 단일 tuple product-prefix crosscheck도 각각 `T=784` boundary `794` skip, `T=800`, `exact_tail_limbs=1`, `q_interval_bound=True`에서 `427694/1917838`, `427153/1915311`, `427533/1917257`, `424851/1904931` vars/clauses로 SAT `0`, UNSAT `1`이었다. 현재 weak full-`x2low7` sanity가 닫힌 high36 next-base는 10개이며 weak-filter assumption 누계는 `5242880`개다.
- 7번 high36 tail-window full-x2 sanity 11-16: 남은 next-base `x1low32=0x788466ff/0xc48466ff/0x928466ff/0x808466ff/0x828466ff/0x858466ff`도 같은 조건에서 base별 `524288`개 assumptions가 모두 SAT `0`, UNSAT였다. weak CNF 크기는 각각 `55548/224287`, `55550/224237`, `55515/224132`, `55419/223671`, `55470/223908`, `55530/224174` vars/clauses다. 단일 tuple product-prefix crosscheck도 각각 `T=784` boundary `794` skip, `T=800`, `exact_tail_limbs=1`, `q_interval_bound=True`에서 `425865/1909628`, `426616/1912917`, `426087/1910467`, `425631/1908200`, `425576/1908038`, `426102/1910530` vars/clauses로 SAT `0`, UNSAT `1`이었다. 이로써 high36 `x6high=0x91548524` 아래의 16개 next-base 전체가 weak full-`x2low7` sanity 기준으로 닫혔고, weak-filter assumption 누계는 `8388608`개다.
- 7번 high36 next-base 16개 safe product-prefix sweep: 같은 high36 `x6high=0x91548524`와 16개 next-base를 weak filter가 아니라 `T-candidates=784,800`, active `T=800`, `exact_tail_limbs=1`, `q_interval_bound=True` product-prefix oracle로 직접 확인했다. `x6low=0..1023`, full `x1high7=0x00..0x7f`, 대표 `x2low7={0x7e,0x7b,0x43,0x11,0x2b}`에서 base별 `655360`개 assumptions가 모두 SAT `0`, UNSAT였고, 네 summary shard 합계는 `10485760`개 UNSAT다. 이는 weak sanity보다 강한 제거 증거지만 여전히 대표 `x2low7` shortlist와 해당 high36 prefix에 한정된다.
- 7번 SAT+CAS low-Coppersmith union proof: `269:2` drop window를 추가한 14-bit union shard proof를 batch57-62로 확장했다. 새로 `6720..8191` completion segment를 16-size shard로 검사했고 모두 hard-eligible `no_roots`, roots/factors `0`이었다. 기존 batch56 aggregate와 합치면 covered completions per variant는 `8896/16384`, coverage는 `54.296875%`, total checked completion cases는 `35584`다. 새 aggregate는 `/tmp/ct07_batch62_14bit_269_nonoverlap_coverage.json`이며 merged range가 `0..8447`까지 확장됐다.
- 7번 SAT+CAS low-Coppersmith union proof 추가: batch63-65로 `8448..9215` segment도 같은 16-size shard 방식으로 검사했고 모두 hard-eligible `no_roots`, roots/factors `0`이었다. 새 aggregate `/tmp/ct07_batch65_14bit_269_nonoverlap_coverage.json` 기준 covered completions per variant는 `9664/16384`, coverage는 `58.984375%`, total checked completion cases는 `38656`이며 merged prefix는 `0..9215`까지 늘었다.
- 7번 SAT+CAS low-Coppersmith union proof 추가 2: batch66-67로 `9216..9727` segment를 더 검사했고 모두 hard-eligible `no_roots`, roots/factors `0`이었다. 새 aggregate `/tmp/ct07_batch67_14bit_269_nonoverlap_coverage.json` 기준 covered completions per variant는 `10176/16384`, coverage는 `62.109375%`, total checked completion cases는 `40704`이며 merged prefix는 `0..9727`까지 늘었다.
- 7번 SAT+CAS low-Coppersmith union proof 추가 3: batch68-70으로 `9728..10495` segment를 더 검사했고 모두 hard-eligible `no_roots`, roots/factors `0`이었다. 새 aggregate `/tmp/ct07_batch70_14bit_269_nonoverlap_coverage.json` 기준 covered completions per variant는 `10944/16384`, coverage는 `66.796875%`, total checked completion cases는 `43776`이며 merged prefix는 `0..10495`까지 늘었다.
- 7번 SAT+CAS low-Coppersmith union proof 추가 4: batch71로 `10496..10751` segment도 검사했고 16개 shard 모두 hard-eligible `no_roots`, roots/factors `0`이었다. 새 aggregate `/tmp/ct07_batch71_14bit_269_nonoverlap_coverage.json` 기준 covered completions per variant는 `11200/16384`, coverage는 `68.359375%`, total checked completion cases는 `44800`이며 merged prefix는 `0..10751`까지 늘었다.
- 7번 SAT+CAS low-Coppersmith union proof 추가 5: batch72-73으로 `10752..11263` segment도 검사했고 모두 hard-eligible `no_roots`, roots/factors `0`이었다. 새 aggregate `/tmp/ct07_batch73_14bit_269_nonoverlap_coverage.json` 기준 covered completions per variant는 `11712/16384`, coverage는 `71.484375%`, total checked completion cases는 `46848`이며 merged prefix는 `0..11263`까지 늘었다.
- 7번 SAT+CAS low-Coppersmith union proof 추가 6: batch74-77로 `11264..12287` segment도 검사했고 모두 hard-eligible `no_roots`, roots/factors `0`이었다. 새 aggregate `/tmp/ct07_batch77_14bit_269_nonoverlap_coverage.json` 기준 covered completions per variant는 `12736/16384`, coverage는 `77.734375%`, total checked completion cases는 `50944`이며 기존 `12288..12351` island와 합쳐 merged prefix가 `0..12351`까지 늘었다.
- 7번 SAT+CAS low-Coppersmith union proof 추가 7: batch78-79로 `12352..12863` segment도 검사했고 모두 hard-eligible `no_roots`, roots/factors `0`이었다. 새 aggregate `/tmp/ct07_batch79_14bit_269_nonoverlap_coverage.json` 기준 covered completions per variant는 `13248/16384`, coverage는 `80.859375%`, total checked completion cases는 `52992`이며 merged prefix는 `0..12863`까지 늘었다.
- 7번 SAT+CAS low-Coppersmith union proof 추가 8: batch80-81로 `12864..13375` segment도 검사했고 모두 hard-eligible `no_roots`, roots/factors `0`이었다. 새 aggregate `/tmp/ct07_batch81_14bit_269_nonoverlap_coverage.json` 기준 covered completions per variant는 `13760/16384`, coverage는 `83.984375%`, total checked completion cases는 `55040`이며 merged prefix는 `0..13375`까지 늘었다.
- 7번 SAT+CAS low-Coppersmith union proof 추가 9: batch82-85로 `13376..14335` segment도 검사했고 모두 hard-eligible `no_roots`, roots/factors `0`이었다. 새 aggregate `/tmp/ct07_batch85_14bit_269_nonoverlap_coverage.json` 기준 covered completions per variant는 `14720/16384`, coverage는 `89.84375%`, total checked completion cases는 `58880`이며 기존 `14336..14463` island와 합쳐 merged prefix는 `0..14463`까지 늘었다.
- 7번 SAT+CAS low-Coppersmith union proof 추가 10: batch86-91로 남은 completion gaps도 모두 닫았다. 최종 aggregate `/tmp/ct07_batch91_14bit_269_nonoverlap_coverage.json` 기준 covered completions per variant는 `16384/16384`, coverage는 `100%`, total checked completion cases는 `65536`, roots/factors `0`이다. 이로써 high branch `x6=0x245521490bd`, `x7=0`, low all-zero union variants 아래 `269:2` minimized 14-bit hard-clause 후보는 전체 completion assignment에서 no-root인 sound no-good branch로 정리됐다.
- 7번 Go Hensel-tail dynamic T / bit-skip 필터: Go exporter가 `T=816,832`를 추가 지원하고, runner/exporter에 `--skip-known-prefix-bits`를 추가했다. full `x6=0x245521490bd`, `x1low32=0`, `x1high7=0`, `x2low7=0x7e`에서 `T=784,800,816,832,848`, `exact_tail_limbs=1`, `q_interval_bound=True`를 모두 검사했고 전부 `SAT=0`, `UNSAT=1`이었다. CNF 크기는 `T=784`의 `382587/1712626` vars/clauses에서 `T=848`의 `469206/2096760`까지 증가했다. 또 `T=784`, `arith_bits=272`, `skip_known_prefix_bits=242`의 작은 CNF로 full `x6` 후보 5개와 representative `x1low32` base 5개를 검사했고, 각 pair의 `x1high7*x2low7=16384` assumptions가 모두 UNSAT였다. 총 `409600` assumptions에서 SAT는 0이고, CNF 크기는 `2620..2642` vars / `5923..6045` clauses 범위였다. 이는 factor 복구는 아니지만, expensive `T>=800` oracle 전에 Go bit-skip 필터를 쓰는 방향을 지지한다.
- 7번 Go bit-skip 54-base 확장: 같은 `T=784`, `arith_bits=272`, `skip_known_prefix_bits=242`, `q_interval_bound=True` 필터를 기존 representative `x1low32` base 54개 전체와 full `x6` 후보 5개 `0x24552149094/097/098/09b/0bd`에 적용했다. 각 `(x6, x1low32)` pair에서 `x1high7*x2low7=16384` assumptions를 검사했고, 총 `5 * 54 * 16384 = 4423680`개 모두 UNSAT였다. SAT는 0, CNF 크기는 `2620..2649` vars / `5923..6073` clauses다. 이 결과는 전체 `x1low32` 공간 proof는 아니지만, 기존 CP-SAT conflict ranking queue의 다섯 full `x6` 후보와 대표 base들의 우선순위를 크게 낮춘다.
- 7번 Go bit-skip 추가 base / low24 branch sweep: 문서에서 추출한 plausible `x1low32` 80개 중 이미 닫은 54개와 잘못 잡힌 `x6` prefix `0x91548524`를 제외하고, 남은 25개 base를 같은 다섯 full `x6` 후보와 교차 검사했다. 추가 `2048000` assumptions 모두 UNSAT였고, 54-base 결과와 합치면 `5 * 79 * 16384 = 6471680` assumptions가 SAT `0`, UNSAT다. 이어 active low24 branch 세 개 `x6=0x245521490bd/x1low24=0x8466ff`, `x6=0x24552149098/x1low24=0x22ffff`, `x6=0x24552149094/x1low24=0xfd44ff`를 각각 256개 `x1low32` 확장 전체로 검사했고, 세 run 합계 `12582912` assumptions 모두 UNSAT였다. SAT 생존자가 없어 더 강한 oracle로 넘길 후보는 없었고, 현재 CP-SAT-ranked 후보군은 우선순위를 더 낮춘다.
- 7번 free-`x1` full-`x6` 필터 정정 전 결과: `run_07_go_sat_filter.py`에 `--free-x1-filter`를 추가했고, 당시에는 다섯 full `x6` 후보가 모두 UNSAT라고 기록했다. 그러나 바로 아래 skip-prefix 버그 때문에 이 UNSAT 결론은 무효다. 수정 후 같은 다섯 후보는 q-bound 유무와 관계없이 SAT `5`, UNSAT `0`이다.
- 7번 free-`x1` `x6high` bucket 필터 정정 전 결과: `run_07_go_sat_filter.py`에 `--free-x1-x6high-filter`를 추가했고, 당시에는 full `x6` 후보 20개와 여러 high-prefix bucket이 모두 UNSAT라고 기록했다. 그러나 바로 아래 skip-prefix 버그 때문에 이 bucket UNSAT 결론은 무효다. 해당 JSON들은 pre-fix 실험 로그로만 남긴다.
- 7번 Go skip-prefix 정정: 위 두 free-`x1` bullet과 이전 Go bit-skip/product-prefix UNSAT 누계에는 `skip_known_prefix_bits` 버그가 섞여 있었다. `addArithmeticPrefixClauses`가 lower prefix carry를 넣은 뒤에도 bit `0..skip-1` target을 계속 강제해 odd `N`에서 empty clause를 만들 수 있었다. loop를 `skipBits`부터 시작하도록 수정하고 empty-clause 회귀 테스트를 추가했다. 수정 후 no fixed `x6`, `T=848`, `arith_bits=272`, `skip_known_prefix_bits=210` sampled branches는 SAT가 됐고, 다섯 full `x6` free-`x1/x2low7` recheck도 q-bound 유무와 관계없이 SAT `5`, UNSAT `0`이다. fixed tuple `arith_bits=320` sanity도 SAT다. 따라서 `skip_known_prefix_bits/limbs`를 사용한 pre-fix UNSAT sweep은 no-good 증거로 쓰면 안 된다. 정정 요약은 `/tmp/ct07_skip_prefix_fix_recheck_summary.json`에 있다.
- 7번 수정 후 post-fix probe: 다섯 full `x6` 후보를 `T=784/800/816/832/848`, `arith_bits=320`, `skip_known_prefix_bits=210`, q interval, odd residues `3/5/7/11`로 다시 검사했지만 모두 SAT `5`, UNSAT `0`이었다. `arith_bits=384` 전체 `x1high7*x2low7=16384` sweep은 180초 timeout이 났고, 한 `x1high7` shard로 줄인 두 테스트도 각각 SAT `128`, UNSAT `0`이었다. CP-SAT exact-tail fixed tuple probe는 10초 내 UNKNOWN이지만 `T=800` tuple에서 conflicts/sec `417.62`까지 올라갔다. `T` build-only 비교에서는 `T=784`가 lower product vars `240`으로 가장 작고 `T=848`은 q prefix inside T가 `79` bits로 늘어나는 대신 lower product vars가 `306`까지 증가했다. 다음 우선순위는 큰 PySAT sweep이 아니라 `T=784/800` 중심의 exact-tail CNF 또는 작은 CP-SAT cube scoring이다.
- 7번 exact-carry `x2` subcube probe: `run_07_go_sat_filter.py`에 `--fix-p-range START:WIDTH:VALUE`를 추가해 `x2=p[265..348]`의 추가 비트를 exporter 단계에서 고정할 수 있게 했다. `x6=0x245521490bd,x1=0x9b183cdcc,x2low7=0x00,T=784,exact_tail_carry_lims=1,exact_carry_bits=272,lowlift_q=272,q interval,odd residues 3/5/7/11` endpoint는 no-extra 상태에서 `380675/1727237` vars/clauses와 180초 solve timeout이었지만, `x2[272..279]=0x00`은 `364643/1655853`으로 UNSAT, `x2[272..287]=0x0000`은 `344644/1568748`로 UNSAT, `x2[272..303]=0x00000000`은 `310427/1417405`로 UNSAT였다. `0xffffffff/0x55555555/0xaaaaaaaa` 32비트 샘플, `0xff/0x55/0xaa` 8비트 샘플, 반대 endpoint `x2low7=0x7f`의 `x2[272..279]=0x00/0xff`도 모두 UNSAT다. 같은 범위를 PySAT assumptions로만 거는 `--assume-p-range`는 q-low/column 상수화를 얻지 못해 대표값 네 개 첫 solve부터 180초 timeout이므로, 다음 pruning은 assumption 재사용보다 fixed `x2` subcube를 exporter에서 생성하는 cube-and-conquer 쪽이 맞다.
- 7번 fixed `x2` subcube sweep 자동화: direct fixed-case 전용 `--fix-p-range-sweep START:WIDTH:all|V1,V2,...`를 runner에 추가했다. 같은 exact-carry endpoint에서 `x2[272..279]=0x00,0x01` smoke는 build/solve가 통과했고, 이어 `x2[272..279]=0x00..0xff` 전체 32개 8-value shards를 실행했다. 모든 shard가 SAT `0`, UNSAT `8`이어서 이 endpoint는 다음 `x2` 8비트 전체 256개 값이 닫혔다. 전체 CNF 크기는 `363939..370359` vars / `1653067..1681769` clauses 범위다. 즉 fixed `x6=0x245521490bd`, `x1=0x9b183cdcc`, `x2low7=0x00` endpoint는 다음 `x2` byte까지 모두 모순이므로, 다음에는 다른 `x2low7`, `x1`, `x6` branch 또는 더 깊은 fixed subcube로 이동한다.
- 7번 `x2low7=0x7f` endpoint sweep: 같은 fixed `x6=0x245521490bd`, `x1=0x9b183cdcc`에서 `x2low7=0x7f`도 `x2[272..279]=0x00..0xff`까지 검사했다. 32개 8-value shards가 모두 SAT `0`, UNSAT `8`이어서 다음 `x2` byte 전체 256개 값이 닫혔다. 전체 집계는 SAT `0`, UNSAT `256`이고, CNF 크기는 `367313..375927` vars / `1669033..1707305` clauses다.
- 7번 `x2low7=0x3f` midpoint sweep: 같은 fixed `x6=0x245521490bd`, `x1=0x9b183cdcc`에서 중간 endpoint `x2low7=0x3f`도 `x2[272..279]=0x00..0xff` 전체를 검사했다. 32개 8-value shards가 모두 SAT `0`, UNSAT `8`이어서 전체 집계는 SAT `0`, UNSAT `256`이고, CNF 크기는 `368578..376682` vars / `1674022..1710360` clauses다. 따라서 이 fixed `x6/x1` exact-carry shard에서 `x2low7=0x00`, `0x3f`, `0x7f` 세 지점이 모두 다음 byte까지 모순이다.
- 7번 `x2low7=0x1f` lower-quarter sweep: 같은 fixed shard에서 `x2low7=0x1f`도 `x2[272..279]=0x00..0xff` 전체를 검사했다. 32개 8-value shards가 모두 SAT `0`, UNSAT `8`이어서 전체 집계는 SAT `0`, UNSAT `256`이고, CNF 크기는 `366022..374311` vars / `1663131..1699757` clauses다. 따라서 이 fixed `x6/x1` exact-carry shard에서는 `x2low7=0x00`, `0x1f`, `0x3f`, `0x7f` 네 지점이 모두 다음 byte까지 모순이다.
- 7번 `x2low7=0x5f` upper-midpoint sweep: 같은 fixed shard에서 `x2low7=0x5f`도 `x2[272..279]=0x00..0xff` 전체를 검사했다. 32개 8-value shards가 모두 SAT `0`, UNSAT `8`이어서 전체 집계는 SAT `0`, UNSAT `256`이고, CNF 크기는 `367210..375805` vars / `1668191..1706414` clauses다. 따라서 이 fixed `x6/x1` exact-carry shard에서는 `x2low7=0x00`, `0x1f`, `0x3f`, `0x5f`, `0x7f` 다섯 지점이 모두 다음 byte까지 모순이다.
- 7번 `x2low7=0x0f` binary-subdivision sweep: 같은 fixed shard에서 `x2low7=0x0f`도 `x2[272..279]=0x00..0xff` 전체를 검사했다. 32개 8-value shards가 모두 SAT `0`, UNSAT `8`이어서 전체 집계는 SAT `0`, UNSAT `256`이고, CNF 크기는 `366700..375375` vars / `1665978..1704481` clauses다. 따라서 이 fixed `x6/x1` exact-carry shard에서는 `x2low7=0x00`, `0x0f`, `0x1f`, `0x3f`, `0x5f`, `0x7f` 여섯 지점이 모두 다음 byte까지 모순이다.
- 7번 `x2low7=0x2f` binary-subdivision sweep: 같은 fixed shard에서 `x2low7=0x2f`도 `x2[272..279]=0x00..0xff` 전체를 검사했다. 32개 8-value shards가 모두 SAT `0`, UNSAT `8`이어서 전체 집계는 SAT `0`, UNSAT `256`이고, CNF 크기는 `368810..376028` vars / `1674932..1707324` clauses다. 따라서 이 fixed `x6/x1` exact-carry shard에서는 `x2low7=0x00`, `0x0f`, `0x1f`, `0x2f`, `0x3f`, `0x5f`, `0x7f` 일곱 지점이 모두 다음 byte까지 모순이다.
- 7번 `x2low7=0x4f` binary-subdivision sweep: 같은 fixed shard에서 `x2low7=0x4f`도 `x2[272..279]=0x00..0xff` 전체를 검사했다. 32개 8-value shards가 모두 SAT `0`, UNSAT `8`이어서 전체 집계는 SAT `0`, UNSAT `256`이고, CNF 크기는 `367579..375110` vars / `1669602..1703400` clauses다. 따라서 이 fixed `x6/x1` exact-carry shard에서는 `x2low7=0x00`, `0x0f`, `0x1f`, `0x2f`, `0x3f`, `0x4f`, `0x5f`, `0x7f` 여덟 지점이 모두 다음 byte까지 모순이다.
- 7번 `x2low7=0x6f` binary-subdivision sweep: 같은 fixed shard에서 `x2low7=0x6f`도 `x2[272..279]=0x00..0xff` 전체를 검사했다. 32개 8-value shards가 모두 SAT `0`, UNSAT `8`이어서 전체 집계는 SAT `0`, UNSAT `256`이고, CNF 크기는 `368670..375186` vars / `1674702..1703761` clauses다. 따라서 이 fixed `x6/x1` exact-carry shard에서는 `x2low7=0x00`, `0x0f`, `0x1f`, `0x2f`, `0x3f`, `0x4f`, `0x5f`, `0x6f`, `0x7f` 아홉 지점이 모두 다음 byte까지 모순이다.
- 7번 `x2low7=0x07` left-subdivision sweep: 같은 fixed shard에서 `x2low7=0x07`도 `x2[272..279]=0x00..0xff` 전체를 검사했다. 32개 8-value shards가 모두 SAT `0`, UNSAT `8`이어서 전체 집계는 SAT `0`, UNSAT `256`이고, CNF 크기는 `367010..375600` vars / `1667190..1705384` clauses다. 따라서 이 fixed `x6/x1` exact-carry shard에서는 `x2low7=0x00`, `0x07`, `0x0f`, `0x1f`, `0x2f`, `0x3f`, `0x4f`, `0x5f`, `0x6f`, `0x7f` 열 지점이 모두 다음 byte까지 모순이다.
- 7번 `x2low7=0x17` left-midpoint sweep: 같은 fixed shard에서 `x2low7=0x17`도 `x2[272..279]=0x00..0xff` 전체를 검사했다. 32개 8-value shards가 모두 SAT `0`, UNSAT `8`이어서 전체 집계는 SAT `0`, UNSAT `256`이고, CNF 크기는 `368173..374631` vars / `1672149..1700990` clauses다. 따라서 이 fixed `x6/x1` exact-carry shard에서는 `x2low7=0x00`, `0x07`, `0x0f`, `0x17`, `0x1f`, `0x2f`, `0x3f`, `0x4f`, `0x5f`, `0x6f`, `0x7f` 열한 지점이 모두 다음 byte까지 모순이다.
- 7번 `x2low7=0x27` midpoint sweep: 같은 fixed shard에서 `x2low7=0x27`도 `x2[272..279]=0x00..0xff` 전체를 검사했다. 32개 8-value shards가 모두 SAT `0`, UNSAT `8`이어서 전체 집계는 SAT `0`, UNSAT `256`이고, CNF 크기는 `367380..375986` vars / `1668852..1707102` clauses다. 따라서 이 fixed `x6/x1` exact-carry shard에서는 `x2low7=0x00`, `0x07`, `0x0f`, `0x17`, `0x1f`, `0x27`, `0x2f`, `0x3f`, `0x4f`, `0x5f`, `0x6f`, `0x7f` 열두 지점이 모두 다음 byte까지 모순이다.
- 7번 `x2low7=0x37` midpoint sweep: 같은 fixed shard에서 `x2low7=0x37`도 `x2[272..279]=0x00..0xff` 전체를 검사했다. 32개 8-value shards가 모두 SAT `0`, UNSAT `8`이어서 전체 집계는 SAT `0`, UNSAT `256`이고, CNF 크기는 `368244..375814` vars / `1672445..1706397` clauses다. 따라서 이 fixed `x6/x1` exact-carry shard에서는 `x2low7=0x00`, `0x07`, `0x0f`, `0x17`, `0x1f`, `0x27`, `0x2f`, `0x37`, `0x3f`, `0x4f`, `0x5f`, `0x6f`, `0x7f` 열세 지점이 모두 다음 byte까지 모순이다.
- 7번 `x2low7=0x47` midpoint sweep: 같은 fixed shard에서 `x2low7=0x47`도 `x2[272..279]=0x00..0xff` 전체를 검사했다. 32개 8-value shards가 모두 SAT `0`, UNSAT `8`이어서 전체 집계는 SAT `0`, UNSAT `256`이고, CNF 크기는 `366557..374823` vars / `1665079..1701978` clauses다. 따라서 이 fixed `x6/x1` exact-carry shard에서는 `x2low7=0x00`, `0x07`, `0x0f`, `0x17`, `0x1f`, `0x27`, `0x2f`, `0x37`, `0x3f`, `0x47`, `0x4f`, `0x5f`, `0x6f`, `0x7f` 열네 지점이 모두 다음 byte까지 모순이다.
- 7번 `x2low7=0x57` midpoint sweep: 같은 fixed shard에서 `x2low7=0x57`도 `x2[272..279]=0x00..0xff` 전체를 검사했다. 32개 8-value shards가 모두 SAT `0`, UNSAT `8`이어서 전체 집계는 SAT `0`, UNSAT `256`이고, CNF 크기는 `367064..374486` vars / `1667632..1700730` clauses다. 따라서 이 fixed `x6/x1` exact-carry shard에서는 `x2low7=0x00`, `0x07`, `0x0f`, `0x17`, `0x1f`, `0x27`, `0x2f`, `0x37`, `0x3f`, `0x47`, `0x4f`, `0x57`, `0x5f`, `0x6f`, `0x7f` 열다섯 지점이 모두 다음 byte까지 모순이다.
- 7번 `x2low7=0x67` upper-side midpoint sweep: 같은 fixed shard에서 `x2low7=0x67`도 `x2[272..279]=0x00..0xff` 전체를 검사했다. 32개 8-value shards가 모두 SAT `0`, UNSAT `8`이어서 전체 집계는 SAT `0`, UNSAT `256`이고, CNF 크기는 `369044..376797` vars / `1676118..1710869` clauses다. 따라서 이 fixed `x6/x1` exact-carry shard에서는 `x2low7=0x00`, `0x07`, `0x0f`, `0x17`, `0x1f`, `0x27`, `0x2f`, `0x37`, `0x3f`, `0x47`, `0x4f`, `0x57`, `0x5f`, `0x67`, `0x6f`, `0x7f` 열여섯 지점이 모두 다음 byte까지 모순이다.
- 7번 `x2low7=0x77` upper-side midpoint sweep: 같은 fixed shard에서 `x2low7=0x77`도 `x2[272..279]=0x00..0xff` 전체를 검사했다. 첫 pass에서는 마지막 4개 shard가 timeout됐지만 `--resume --timeout-seconds 900` 재시도로 닫혔고, 최종 집계는 SAT `0`, UNSAT `256`, CNF 크기 `367701..376326` vars / `1670461..1708781` clauses다. 따라서 이 fixed `x6/x1` exact-carry shard에서는 `x2low7=0x00`, `0x07`, `0x0f`, `0x17`, `0x1f`, `0x27`, `0x2f`, `0x37`, `0x3f`, `0x47`, `0x4f`, `0x57`, `0x5f`, `0x67`, `0x6f`, `0x77`, `0x7f` 열일곱 지점이 모두 다음 byte까지 모순이다.
- 7번 `x0+x1+x2` 12-bit low-C union proof: fixed `x6=0x245521490bd`, `x7=0` 및 같은 base-selected low assignment guard 아래에서 drop windows `150:2`, `152:2`, `210:2`, `212:2`, `267:2`, `269:2`를 8개 shard로 모두 검사했다. 전체 coverage는 `4096/4096`, unique oracle case는 `4096`, total completion checks는 `16384`였고 모두 hard-eligible `no_roots`, roots/factors `0`이었다. 따라서 `p[150..153]`, `p[210..213]`, `p[267..270]` 열두 literal을 함께 drop하는 193-literal no-good shape는 sound하다.
- 7번 `x0+x1+x2+x3` 16-bit low-C union pilot: 위 12-bit union에 x3 low-side `p[362..365]`까지 더한 첫 스물여섯 shard를 검사했다. completion prefix `0..13311`이 모두 hard-eligible `no_roots`였고, coverage는 `13312/65536`, unique oracle case `13312`, total completion checks `53248`, roots/factors `0`, remaining common literal count `189`다. 아직 full 16-bit proof는 아니며 다음 shard는 completion `13312`부터 실행 중이다.
- 7번 `x0+x1+x2+x3` 16-bit low-C union pilot 추가: `13312..13823` shard도 모두 hard-eligible `no_roots`로 통과했다. 현재 coverage는 `13824/65536 = 0.2109375`, input shards `27`, unique oracle case `13824`, total completion checks `55296`, roots/factors `0`, remaining common literal count `189`다. 다음 shard `13824..14336`은 같은 fixed ranges로 실행 중이다.
- 7번 `x2low7=0x03` fine-grid exact-carry sweep: 같은 fixed shard에서 `x2low7=0x03`도 `x2[272..279]=0x00..0xff` 전체가 닫혔다. 최종 집계는 SAT `0`, UNSAT `256`, CNF `368154..373187` vars / `1672183..1694677` clauses다. 이로써 해당 fixed `x6/x1` exact-carry shard에서 next byte까지 모순인 sampled `x2low7` 지점은 `0x00`, `0x03`, `0x07`, `0x0f`, `0x17`, `0x1f`, `0x27`, `0x2f`, `0x37`, `0x3f`, `0x47`, `0x4f`, `0x57`, `0x5f`, `0x67`, `0x6f`, `0x77`, `0x7f` 열여덟 개다.
- 7번 alt94 branch full exact-carry sweep: 대체 branch `x6=0x24552149094`, `x1=0x166f09bd14`, `x2low7=0x78`에서 `x2[272..279]=0x00..0xff` 256개 값을 검사했고 SAT `0`, UNSAT `256`, CNF `366594..373052` vars / `1665106..1693947` clauses였다. 이는 해당 alternate branch의 next byte 전체를 q272 exact-carry 조건에서 배제한다.
- 7번 `x2low7=0x0b` exact-carry full sweep: 같은 fixed `x6=0x245521490bd`, `x1=0x9b183cdcc`에서 off-grid `x2low7=0x0b`도 full next-byte sweep을 닫았다. `x2[272..279]=0x00..0xff` 256개 값이 SAT `0`, UNSAT `256`, CNF `367194..375376` vars / `1668164..1704231` clauses였고, sat/factor rows는 0이다.
- 7번 `x2low7=0x13` partial exact-carry sweep: 같은 fixed shard에서 다음 off-grid `x2low7=0x13`도 full next-byte sweep으로 승격했다. 현재 `x2[272..279]=0x00..0xaf` 176개 값이 SAT `0`, UNSAT `176`, CNF `368385..374096` vars / `1673008..1698705` clauses로 닫혔고, runner가 `0xb0`부터 계속 진행 중이다.
- 7번 `x2low7=0x1b` partial exact-carry sweep: 같은 fixed shard에서 nearest unclosed off-grid point `x2low7=0x1b`도 시작했다. 현재 `x2[272..279]=0x00..0x67` 104개 값이 SAT `0`, UNSAT `104`, CNF `366490..373677` vars / `1664882..1696760` clauses로 닫혔고, runner가 `0x68`부터 계속 진행 중이다.
- 7번 exact-carry partial sweep 추가 진행: `x2low7=0x13`은 현재 `x2[272..279]=0x00..0xcf` 208개 값이 SAT `0`, UNSAT `208`로 닫혔고 다음 missing value는 `0xd0`이다. `x2low7=0x1b`은 `0x00..0x7f` 128개 값이 SAT `0`, UNSAT `128`로 닫혔고 다음 missing value는 `0x80`이다. SAT/factor row는 없었다.
- 7번 q-ranked full-x5 verifier: full x5 beam은 `682:87:0x40c04141804040c140c1`로 수렴했고 `q_low=265`, `q_prefix=355`, `q_known=620`이었다. direct folded-Coron verifier는 primitive margin `144.0`, reconstructed polynomial `13`개였지만 root 0, verified factor 0이었다. 출력은 `/tmp/ct07_q_x5_full87_x1_9b_beam8_pp32_20260604.json` 및 `/tmp/ct07_edge_oracle_fullx5_682_87_x1_9b_20260604.json`이다.
- 7번 x0/x7 full-x5 q-ranking sweep: fixed `x1=0x9b183cdcc`, x0 전체, `x7={0,4,8}`에서도 ceiling은 `q_prefix=355`, `q_known=620`, `q_low=265`에 머물렀다. best tie는 `x7=4`, `682:87:0x1c1c081800000400001`였고, `x0=0,x7=4` direct folded-Coron verifier도 margin `144.0`, reconstructed polynomial 13개, root 0, verified factor 0이었다. 출력은 `/tmp/ct07_q_x5_x0all_x7_0_4_8_x1_9b_w87_20260604.json` 및 `/tmp/ct07_edge_oracle_fullx5_x7_4_x0_0_x1_9b_20260604.json`이다.
- 7번 2026-06-04 exact free-x1 전환: `x0+x1+x2+x3` 16-bit low-C union은 `13824..14848`까지 확장되어 coverage `14848/65536 = 0.2265625`, roots/factors 0이지만, 더 강한 exact free-`x1` filter가 nearby full-x6 shortlist `0x24552149094/097/098/09b/0bd` 전체를 UNSAT로 닫았다. 조건은 `T=784`, `arith_bits=272`, `skip_known_prefix_bits=208`, `--lowlift-q 272`, q interval, odd residues `3/5/7/11`, `--exact-tail-carry-limbs 1`, `--exact-carry-bits 272`이며 candidate별 CNF는 약 `518170..518260` vars / `2329135..2329555` clauses, SAT false, model 0이다. 같은 fixed shard에서 `x2low7=0x13` next byte도 full closure(SAT 0, UNSAT 256)됐지만, full-x6 shortlist가 닫혔으므로 이 branch의 byte/low-C extension priority는 내려간다. all-x7 full-x5 q-ranking은 256 branch에서도 `q_prefix=355`, `q_known=620`, `q_low=265` ceiling을 못 넘겼고 best `x7=7`, x5 `682:87:0x8080404041804002` Coron verifier도 root/factor 0이었다.
- 7번 corrected high32 bucket pruning: q272 exact-carry free-`x1/x2low7/x6low14` filter를 `T=800`, `skip_known_prefix_bits=208`, `x6high_bits=32`로 다시 적용했고, 추적 high32 bucket `0x9154852/0x40060/0x50010/0x40010/0xcf49080/0x5d55090/0x90070` 일곱 개가 모두 SAT false, model 0으로 닫혔다. CNF 크기는 `546188..546334` vars / `2455058..2455767` clauses이며 출력은 `/tmp/ct07_free_x1_exactcarry1_x6high32_9154852_q272_20260604.stdout`, `/tmp/ct07_free_x1_exactcarry1_x6high32_x7rank_rest_q272_20260604.stdout`이다. 이어 fast-tail diverse high32 후보 `0x00010006/0x000b0004/0x00150001/0x001e0008/0x00280005/0x00320003/0x003c0000/0x00450006/0x004f0004/0x00590001/0x00620008/0x006c0005/0x00760002/0x00800000/0x00890006/0x00930004`도 모두 SAT false, model 0으로 닫혔다. 이 16개 aggregate는 SAT `0`, UNSAT `16`, CNF `546144..546235` vars / `2454900..2455245` clauses이고 `/tmp/ct07_free_x1_exactcarry1_x6high32_fasttop16_verified_20260604.json`에 있다. corrected high32 pruning 누계는 `23 * 2^14 = 376832`개 full `x6` 값이지만, 아직 factor/plaintext 복구는 아니다.
- 7번 high20 wider-prefix exact closure: 같은 corrected q272 exact-carry filter로 high28/high24/high20 fast-tail top4를 확인했고, high28/high24는 high20 parent 안에 들어가는 child-prefix audit으로만 둔다. unique coverage는 high20 기준으로 세며 fast-tail high32 top16 queue가 유도하는 high20 parent `0x10/0xb0/0x150/0x1e0/0x280/0x320/0x3c0/0x450/0x4f0/0x590/0x620/0x6c0/0x760/0x800/0x890/0x930` 전체가 SAT false, model 0이다. `0x620`은 direct complete Go export와 streaming PySAT solve로 검증했다. CNF 크기는 `600632..600889` vars / `2696607..2697749` clauses이고, corrected aggregate는 `/tmp/ct07_high20_top16_exact_closure_corrected_20260604.json`이다. unique pruning은 `16 * 2^26 = 1073741824`개 full `x6` 값이며 factor/plaintext 복구는 아니다.
- 7번 high20 top24 exact closure: corrected q272 exact-carry high20 parent closure를 top16 이후 `0x9d0/0xa60/0xb00/0xba0`와 `0xc40/0xcd0/0xd70/0xe10`까지 확장했고, next8 전체가 SAT false, model 0이다. 두 번째 next4 CNF는 `600733..600866` vars / `2697021..2697668` clauses이고 `/tmp/ct07_high20_next4b_exact_closure_20260604.json`에 기록했다. combined top24 aggregate는 `/tmp/ct07_high20_top24_exact_closure_20260604.json`이며 unique pruning은 `24 * 2^26 = 1610612736`개 full `x6` 값이다. factor/plaintext 복구는 아니다.
- 7번 high20 top28 / x1wide24 follow-up: 다음 high20 parent `0xeb0/0xf40/0xfe0/0x1080`도 corrected q272 exact-carry 조건에서 SAT false, model 0으로 닫혔다. 세 번째 next4 CNF는 `600707..600814` vars / `2696878..2697355` clauses이고 `/tmp/ct07_high20_next4c_exact_closure_20260604.json`에 기록했다. combined top28 aggregate는 `/tmp/ct07_high20_top28_exact_closure_20260604.json`이며 unique pruning은 `28 * 2^26 = 1879048192`개 full `x6` 값이다. p-only `x1wide24`는 top `q_low/q_prefix/q_known=234/194/428`로 `x1wide20`보다 4비트 개선됐지만 Hensel top4 `224/240/256/272`, 2000ms는 `sat=8`, `unknown=8`, `unsat=0`이라 hard no-good은 아니다. factor/plaintext 복구는 아니다.
- 7번 high20 top32 / x1wide28 follow-up: 다음 high20 parent `0x1110/0x11b0/0x1250/0x12f0`도 corrected q272 exact-carry 조건에서 SAT false, model 0으로 닫혔다. 네 번째 next4 CNF는 `600720..600851` vars / `2696899..2697559` clauses이고 `/tmp/ct07_high20_next4d_exact_closure_20260604.json`에 기록했다. combined top32 aggregate는 `/tmp/ct07_high20_top32_exact_closure_20260604.json`이며 unique pruning은 `32 * 2^26 = 2147483648`개 full `x6` 값이다. p-only `x1wide28`은 top `q_low/q_prefix/q_known=238/194/432`로 `x1wide24`보다 4비트 개선됐지만 Hensel top4 `224/240/256/272`, 2000ms는 `sat=8`, `unknown=8`, `unsat=0`이라 hard no-good은 아니다. factor/plaintext 복구는 아니다.
- 7번 high20 top40 / x1wide36 follow-up: 다음 high20 parent `0x1380/0x1420/0x14c0/0x1550/0x15f0/0x1690/0x1730/0x17c0`도 corrected q272 exact-carry 조건에서 SAT false, model 0으로 닫혔다. 새 두 next4 CNF는 `600734..600845` vars / `2697066..2697614` clauses이고 `/tmp/ct07_high20_next4e_exact_closure_20260604.json`, `/tmp/ct07_high20_next4f_exact_closure_20260604.json`에 기록했다. combined top40 aggregate는 `/tmp/ct07_high20_top40_exact_closure_20260604.json`이며 unique pruning은 `40 * 2^26 = 2684354560`개 full `x6` 값이다. p-only `x1wide36`은 top `q_low/q_prefix/q_known=246/194/440`까지 개선됐지만 Hensel top4 `224/240/256/272`, 2000ms는 `sat=12`, `unknown=4`, `unsat=0`이라 hard no-good은 아니다. factor/plaintext 복구는 아니다.
- 7번 low-C 29.7% / x1wide39 / x0x7 edge follow-up: 16-bit low-C union proof는 `18944..19456` shard도 통과해 contiguous coverage `19456/65536 = 0.296875`, total completion checks `77824`, roots/factors `0`이 되었다(`/tmp/ct07_union_x0_x1_x2_x3_16bit_aggregate_after_19456.json`). p-only full zero `x1` (`210:39`)은 top `q_low/q_prefix/q_known=273/194/467`까지 올라갔지만 Hensel top4 `224/240/256/272`, 2000ms가 전부 SAT(`sat=16`)라 hard no-good은 아니다. `x0={1,2}`, all `x7` edge/q-ranking도 best `x7=10`에서 기존 ceiling `265/355/620` 그대로였고 folded-Coron verifier는 root/factor 0이었다. factor/plaintext 복구는 아니다.
- 7번 high20 top44 / direct edge coverage supplement: high20 ranking source를 top80까지 새로 만들고 first 40이 기존 top40과 일치함을 확인한 뒤 ranks 41..44 `0x1860/0x1900/0x19a0/0x1a30`도 corrected q272 exact-carry 조건에서 SAT false, model 0으로 닫았다. combined top44 aggregate는 `/tmp/ct07_high20_top44_exact_closure_20260604.json`이고 unique pruning은 `44 * 2^26 = 2952790016`개 full `x6` 값이다. `210:40:0`은 x1 바깥 known bit `p[249]=1`과 충돌하므로 유효한 p-only 확장이 아니다. 누락됐던 direct `x0=0`, `x7=0,4,7` coverage도 보강했지만 best `q_low/q_prefix/q_known=265/303/568`로 기존 ceiling보다 약했고 folded-Coron root/factor는 0이었다. factor/plaintext 복구는 아니다.
- 7번 low-C 30.5% / high20 top48 / deeper p-only / x0=3,4 edge follow-up: low-C union proof는 `19456..19968` shard도 통과해 contiguous coverage `19968/65536 = 0.3046875`, total completion checks `79872`, roots/factors `0`이 되었다(`/tmp/ct07_union_x0_x1_x2_x3_16bit_aggregate_after_19968.json`). high20 ranks 45..48 `0x1ad0/0x1b70/0x1c00/0x1ca0`도 SAT false, model 0으로 닫혀 combined top48은 `48 * 2^26 = 3221225472`개 full `x6` 값을 제거했다(`/tmp/ct07_high20_top48_exact_closure_20260604.json`). `x1wide39` deeper Hensel `288/304/320/336` bits는 기본 top4 전부 unknown이고, `x2w12` q-growth는 `q_low/q_prefix/q_known=277/194/471`까지 올랐지만 Hensel은 `sat=2`, `unknown=14`라 hard no-good은 아니다. `x0=3,4`, all `x7` edge/q-ranking도 best `x7=10`, x5 `682:87:0x820181c20002018148`, `q_low/q_prefix/q_known=265/355/620`에 머물렀고 folded-Coron root/factor는 0이었다. factor/plaintext 복구는 아니다.
- 7번 low-C 31.25% / high20 top52 / x2w16 p-only / x0=5,6 edge follow-up: low-C union proof는 `19968..20480` shard도 통과해 contiguous coverage `20480/65536 = 0.3125`, total completion checks `81920`, roots/factors `0`이 되었다(`/tmp/ct07_union_x0_x1_x2_x3_16bit_aggregate_after_20480.json`). high20 ranks 49..52 `0x1d40/0x1de0/0x1e70/0x1f10`도 SAT false, model 0으로 닫혀 combined top52는 `52 * 2^26 = 3489660928`개 full `x6` 값을 제거했다(`/tmp/ct07_high20_top52_exact_closure_20260604.json`). p-only는 full-zero `x1wide39`에서 x2를 16비트로 넓혀 `q_low/q_prefix/q_known=281/194/475`까지 올렸지만 Hensel `280/288/296/304/320` bits는 `sat=8`, `unknown=12`라 hard no-good은 아니다. `x0=5,6`, all `x7` edge/q-ranking도 best `x7=10`, x5 `682:87:0x820181c20002018148`, `q_low/q_prefix/q_known=265/355/620`에 머물렀고 folded-Coron root/factor는 0이었다. factor/plaintext 복구는 아니다.
- 7번 low-C 32.03% / high20 top56 / x2w20 p-only / x0=7,8 edge follow-up: low-C union proof는 `20480..20992` shard도 통과해 contiguous coverage `20992/65536 = 0.3203125`, total completion checks `83968`, roots/factors `0`이 되었다(`/tmp/ct07_union_x0_x1_x2_x3_16bit_aggregate_after_20992.json`). high20 ranks 53..56 `0x1fb0/0x2040/0x20e0/0x2180`도 SAT false, model 0으로 닫혀 combined top56은 `56 * 2^26 = 3758096384`개 full `x6` 값을 제거했다(`/tmp/ct07_high20_top56_exact_closure_20260604.json`). p-only는 full-zero `x1wide39`에서 x2를 20비트로 넓혀 `q_low/q_prefix/q_known=285/194/479`까지 올렸지만 Hensel `288/296/304/320/336` bits는 `sat=8`, `unknown=12`라 hard no-good은 아니다. `x0=7,8`, all `x7` edge/q-ranking도 best `x7=10`, x5 `682:87:0x820181c20002018148`, `q_low/q_prefix/q_known=265/355/620`에 머물렀고 folded-Coron root/factor는 0이었다. factor/plaintext 복구는 아니다.
- 7번 low-C 32.81% / high20 top60 / x2w24 p-only follow-up: low-C union proof는 `20992..21504` shard도 통과해 contiguous coverage `21504/65536 = 0.328125`, total completion checks `86016`, roots/factors `0`이 되었다(`/tmp/ct07_union_x0_x1_x2_x3_16bit_aggregate_after_21504.json`). high20 ranks 57..60 `0x2220/0x22b0/0x2350/0x23f0`도 SAT false, model 0으로 닫혀 combined top60은 `60 * 2^26 = 4026531840`개 full `x6` 값을 제거했다(`/tmp/ct07_high20_top60_exact_closure_20260604.json`). p-only는 full-zero `x1wide39`에서 x2를 24비트로 넓혀 `q_low/q_prefix/q_known=289/194/483`까지 올렸지만 Hensel `296/304/320/336/352` bits는 `sat=4`, `unknown=16`이라 hard no-good은 아니다. factor/plaintext 복구는 아니다.
- 7번 low-C 33.59% / high20 top64 / x2w28 / mixed p/q follow-up: low-C union proof는 `21504..22016` shard도 통과해 contiguous coverage `22016/65536 = 0.3359375`, total completion checks `88064`, roots/factors `0`이 되었다(`/tmp/ct07_union_x0_x1_x2_x3_16bit_aggregate_after_22016.json`). high20 ranks 61..64 `0x2490/0x2520/0x25c0/0x2660`도 SAT false, model 0으로 닫혀 combined top64는 `64 * 2^26 = 4294967296`개 full `x6` 값을 제거했다(`/tmp/ct07_high20_top64_exact_closure_20260604.json`). p-only는 full-zero `x1wide39`에서 x2를 28비트로 넓혀 `q_low/q_prefix/q_known=293/194/487`까지 올렸지만 Hensel `304/320/336/352/368` bits는 `sat=3`, `unknown=17`이라 hard no-good은 아니다. mixed p/q sampled evaluator smoke도 q-term row 4개가 모두 projection-derived이고 sampled prune 0이라 아직 pruning oracle은 아니다. factor/plaintext 복구는 아니다.
- 7번 low-C 34.38% / high20 top68 / x2w32 / mixed p/q follow-up: low-C union proof는 `22016..22528` shard도 통과해 contiguous coverage `22528/65536 = 0.34375`, total completion checks `90112`, roots/factors `0`이 되었다(`/tmp/ct07_union_x0_x1_x2_x3_16bit_aggregate_after_22528.json`). high20 ranks 65..68 `0x26f0/0x2790/0x2830/0x28d0`도 SAT false, model 0으로 닫혀 combined top68은 `68 * 2^26 = 4563402752`개 full `x6` 값을 제거했다(`/tmp/ct07_high20_top68_exact_closure_20260604.json`). p-only는 full-zero `x1wide39`에서 x2를 32비트로 넓혀 `q_low/q_prefix/q_known=297/194/491`까지 올렸지만 Hensel `304/320/336/352/368` bits는 `sat=4`, `unknown=16`이라 hard no-good은 아니다. mixed p/q q150w64/x0x1x7 sampled evaluator도 projection-derived 후보뿐이고 sampled prune 0이라 아직 pruning oracle은 아니다. factor/plaintext 복구는 아니다.
- 7번 low-C 35.16% / high20 top72 / x2w36 / multi-q mixed p/q follow-up: low-C union proof는 `22528..23040` shard도 통과해 contiguous coverage `23040/65536 = 0.3515625`, total completion checks `92160`, roots/factors `0`이 되었다(`/tmp/ct07_union_x0_x1_x2_x3_16bit_aggregate_after_23040.json`). high20 ranks 69..72 `0x2960/0x2a00/0x2aa0/0x2b30`도 SAT false, model 0으로 닫혀 combined top72는 `72 * 2^26 = 4831838208`개 full `x6` 값을 제거했다(`/tmp/ct07_high20_top72_exact_closure_20260604.json`). p-only x2w36은 `q_low/q_prefix/q_known=301/194/495`까지 올랐지만 Hensel은 `sat=4`, `unknown=16`이라 hard no-good은 아니다. multi-q mixed smoke `150:32,214:32`는 sampled relation이 product-prefix samples를 전부 reject하는 첫 diagnostic signal을 냈지만 omitted q gap/p bits 때문에 sound no-good은 아니다. factor/plaintext 복구는 아니다.
- 7번 low-C 35.94% / high20 top76 / x2w40 follow-up: low-C union proof는 `23040..23552` shard도 통과해 contiguous coverage `23552/65536 = 0.359375`, total completion checks `94208`, roots/factors `0`이 되었다(`/tmp/ct07_union_x0_x1_x2_x3_16bit_aggregate_after_23552.json`). high20 ranks 73..76 `0x2bd0/0x2c70/0x2d10/0x2da0`도 SAT false, model 0으로 닫혀 combined top76은 `76 * 2^26 = 5100273664`개 full `x6` 값을 제거했다(`/tmp/ct07_high20_top76_exact_closure_20260604.json`). p-only x2w40은 `q_low/q_prefix/q_known=305/194/499`까지 올랐지만 Hensel은 `sat=5`, `unknown=19`라 hard no-good은 아니다. factor/plaintext 복구는 아니다.
- 7번 low-C 36.72% / high20 top80 / x2w44 follow-up: low-C union proof는 `23552..24064` shard도 통과해 contiguous coverage `24064/65536 = 0.3671875`, total completion checks `96256`, roots/factors `0`이 되었다(`/tmp/ct07_union_x0_x1_x2_x3_16bit_aggregate_after_24064.json`). high20 ranks 77..80 `0x2e40/0x2ee0/0x2f80/0x3010`도 SAT false, model 0으로 닫혀 current top80은 `80 * 2^26 = 5368709120`개 full `x6` 값을 제거했다(`/tmp/ct07_high20_top80_exact_closure_20260604.json`). p-only x2w44는 `q_low/q_prefix/q_known=309/194/503`까지 올랐지만 Hensel은 `sat=7`, `unknown=21`이라 hard no-good은 아니다. contiguous multi-q smoke도 gap 0이면 sample prune 0이라 q-gap artifact 판단을 강화한다. factor/plaintext 복구는 아니다.
- 7번 low-C/high20/x2w48 추가: `x0+x1+x2+x3` 16-bit low-C union은 `24576/65536 = 37.5%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 top96까지 모두 UNSAT가 되어 `96 * 2^26 = 6442450944`개 full `x6` 값을 parent 단위로 닫았다. p-only full-zero `x1wide39` + `x2w48`은 `q_low/q_prefix/q_known=313/194/507`까지 개선됐지만 Hensel `304..416` bits는 `sat=8`, `unknown=24`, `unsat=0`이라 아직 hard no-good은 아니다.
- 7번 low-C/high20/x2w56 추가: `x0+x1+x2+x3` 16-bit low-C union은 `26112/65536 = 39.84375%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top120 source 기준 top112까지 모두 UNSAT가 되어 `112 * 2^26 = 7516192768`개 full `x6` 값을 parent 단위로 닫았다. p-only full-zero `x1wide39` + `x2w56`은 `q_low/q_prefix/q_known=321/194/515`까지 개선됐지만 Hensel `304..448` bits는 `sat=9`, `unknown=31`, `unsat=0`이라 아직 hard no-good은 아니다.
- 7번 low-C/high20/x2w64 추가: `x0+x1+x2+x3` 16-bit low-C union은 `26624/65536 = 40.625%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top120 source 기준 top120까지 모두 UNSAT가 되어 `120 * 2^26 = 8053063680`개 full `x6` 값을 parent 단위로 닫았다. p-only full-zero `x1wide39` + `x2w64`은 `q_low/q_prefix/q_known=329/194/523`까지 개선됐지만 Hensel `304..480` bits는 `sat=12`, `unknown=36`, `unsat=0`이라 아직 hard no-good은 아니다.
- 7번 low-C/high20/x2w72 추가: `x0+x1+x2+x3` 16-bit low-C union은 `27648/65536 = 42.1875%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top136 source 기준 top136까지 모두 UNSAT가 되어 `136 * 2^26 = 9126805504`개 full `x6` 값을 parent 단위로 닫았다. p-only full-zero `x1wide39` + `x2w72`는 `q_low/q_prefix/q_known=337/194/531`까지 개선됐지만 Hensel `304..512` bits는 `sat=12`, `unknown=44`, `unsat=0`이라 아직 hard no-good은 아니다.
- 7번 low-C/high20/x2w80 추가: `x0+x1+x2+x3` 16-bit low-C union은 `28160/65536 = 42.96875%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top152 source 기준 top152까지 모두 UNSAT가 되어 `152 * 2^26 = 10200547328`개 full `x6` 값을 parent 단위로 닫았다. p-only full-zero `x1wide39` + `x2w80`은 `q_low/q_prefix/q_known=345/194/539`까지 개선됐지만 Hensel `304..544` bits는 `sat=16`, `unknown=48`, `unsat=0`이라 아직 hard no-good은 아니다.
- 7번 low-C/high20/x2w84 추가: `x0+x1+x2+x3` 16-bit low-C union은 `28672/65536 = 43.75%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top168 source 기준 top168까지 모두 UNSAT가 되어 `168 * 2^26 = 11274289152`개 full `x6` 값을 parent 단위로 닫았다. p-only full-zero `x1wide39` + 유효한 끝점 `x2w84`는 `q_low/q_prefix/q_known=362/194/556`까지 개선됐지만 Hensel `304..560` bits는 `sat=20`, `unknown=48`, `unsat=0`이라 아직 hard no-good은 아니다. `x2w88`은 known-one bits와 충돌하고, mixed q150w256/Sumset degree 10/unknown-divisor preflight도 새 pruning 신호가 없었다.
- 7번 low-C/high20 top184 추가: `x0+x1+x2+x3` 16-bit low-C union은 `29184/65536 = 44.53125%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top184 source 기준 top184까지 모두 UNSAT가 되어 `184 * 2^26 = 12348030976`개 full `x6` 값을 parent 단위로 닫았다. Sumset degree 11 sidecar와 cap3000/cap1000/cap200 retries는 0-byte output이라 중단했고 usable signal로 세지 않는다.
- 7번 low-C/high20 top200 추가: `x0+x1+x2+x3` 16-bit low-C union은 `29696/65536 = 45.3125%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top200 source 기준 top200까지 모두 UNSAT가 되어 `200 * 2^26 = 13421772800`개 full `x6` 값을 parent 단위로 닫았다. p-only x3w12는 `q_low/q_prefix/q_known=374/194/568`까지 개선됐지만 chunked Hensel `304..576` bits는 `sat=23`, `unknown=49`, `unsat=0`이라 hard no-good은 아니다.
- 7번 low-C/high20 top216 추가: `x0+x1+x2+x3` 16-bit low-C union은 `30208/65536 = 46.09375%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top216 source 기준 top216까지 모두 UNSAT가 되어 `216 * 2^26 = 14495514624`개 full `x6` 값을 parent 단위로 닫았다. p-only x3w20은 `q_low/q_prefix/q_known=382/194/576`까지 개선됐지만 chunked Hensel `304..576` bits는 `sat=24`, `unknown=48`, `unsat=0`이라 hard no-good은 아니다.
- 7번 low-C 추가: `x0+x1+x2+x3` 16-bit low-C union은 `30720/65536 = 46.875%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. 남은 missing range는 `30720..65536`이다.
- 7번 low-C/high20 top232/q-known 600 추가: `x0+x1+x2+x3` 16-bit low-C union은 `31232/65536 = 47.65625%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top232 source 기준 top232까지 모두 UNSAT가 되어 `232 * 2^26 = 15569256448`개 full `x6` 값을 parent 단위로 닫았다. p-only best는 `q_low/q_prefix/q_known=394/206/600`까지 개선됐지만 chunked Hensel `304..608` bits는 `sat=28`, `unknown=52`, `unsat=0`이라 hard no-good은 아니다.
- 7번 low-C/high20 top248/q-known 616 추가: `x0+x1+x2+x3` 16-bit low-C union은 `31744/65536 = 48.4375%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top248 source 기준 top248까지 모두 UNSAT가 되어 `248 * 2^26 = 16642998272`개 full `x6` 값을 parent 단위로 닫았다. p-only best는 `q_low/q_prefix/q_known=402/214/616`까지 개선됐지만 chunked Hensel `304..640` bits는 `sat=28`, `unknown=60`, `unsat=0`이라 hard no-good은 아니다. edge x1-alt/all-x7 verifier도 root/factor 0이었다.
- 7번 low-C/high20 top264/q-known 632 추가: `x0+x1+x2+x3` 16-bit low-C union은 `32256/65536 = 49.21875%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top264 source 기준 top264까지 모두 UNSAT가 되어 `264 * 2^26 = 17716740096`개 full `x6` 값을 parent 단위로 닫았다. p-only best는 `q_low/q_prefix/q_known=410/222/632`까지 개선됐지만 first-base top4 Hensel `304..704` bits는 `sat=33`, `unknown=71`, `unsat=0`이라 hard no-good은 아니다.
- 7번 low-C/high20 top280/q-known 648 추가: `x0+x1+x2+x3` 16-bit low-C union은 `32768/65536 = 50%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top280 source 기준 top280까지 모두 UNSAT가 되어 `280 * 2^26 = 18790481920`개 full `x6` 값을 parent 단위로 닫았다. p-only best는 `q_low/q_prefix/q_known=418/230/648`까지 개선됐지만 top4 Hensel `320..736` bits는 `sat=29`, `unknown=79`, `unsat=0`이라 hard no-good은 아니다.
- 7번 low-C/high20 top296/q-known 664 추가: `x0+x1+x2+x3` 16-bit low-C union은 `33280/65536 = 50.78125%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top296 source 기준 top296까지 모두 UNSAT가 되어 `296 * 2^26 = 19864223744`개 full `x6` 값을 parent 단위로 닫았다. p-only best는 `q_low/q_prefix/q_known=426/238/664`까지 개선됐지만 top4 Hensel `336..768` bits는 `sat=28`, `unknown=84`, `unsat=0`이라 hard no-good은 아니다.
- 7번 low-C/high20 top312/q-known 689 추가: `x0+x1+x2+x3` 16-bit low-C union은 `33792/65536 = 51.5625%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top312 source 기준 top312까지 모두 UNSAT가 되어 `312 * 2^26 = 20937965568`개 full `x6` 값을 parent 단위로 닫았다. p-only best는 `q_low/q_prefix/q_known=434/255/689`까지 개선됐지만 valid cube가 `7640/577664`뿐이고 top4 Hensel `352..800` bits는 `sat=27`, `unknown=89`, `unsat=0`이라 hard no-good은 아니다.
- 7번 low-C/high20 top328/q-known 856 추가: `x0+x1+x2+x3` 16-bit low-C union은 `34304/65536 = 52.34375%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top328 source 기준 top328까지 모두 UNSAT가 되어 `328 * 2^26 = 22011707392`개 full `x6` 값을 parent 단위로 닫았다. p-only full4 q-growth는 `q_low/q_prefix/q_known=600/256/856`까지 개선됐고 valid cube는 `1616/577664`, inconsistent skip은 `576048`이었다. streamed top4 Hensel `600,608,624,...,800` bits는 `sat=8`, `unknown=48`, `unsat=0`이라 hard no-good은 아니다.
- 7번 low-C/high20 top344/q-known 864 추가: `x0+x1+x2+x3` 16-bit low-C union은 `34816/65536 = 53.125%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top344 source 기준 top344까지 모두 UNSAT가 되어 `344 * 2^26 = 23085449216`개 full `x6` 값을 parent 단위로 닫았다. p-only q-growth는 `q_low/q_prefix/q_known=600/264/864`까지 개선됐지만 valid cube는 `2052/17044752`뿐이고 guarded top4 Hensel `600,616,632,...,792` bits는 `sat=8`, `unknown=34`, `process_timeout=10`, `unsat=0`이라 hard no-good은 아니다.
- 7번 low-C/high20 top360/q-known 880 추가: 16-bit low-C union은 `35328/65536 = 53.90625%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top360 source 기준 모두 UNSAT가 되어 `360 * 2^26 = 24159191040`개 full `x6` 값을 parent 단위로 닫았다. p-only q-growth는 `q_low/q_prefix/q_known=600/280/880`까지 개선됐고 guarded top4 Hensel `600,616,632,...,792` bits는 `sat=7`, `unknown=31`, `process_timeout=14`, `unsat=0`이라 hard no-good은 아니다. q888 sidecar는 아직 Hensel 검증 전이다.
- 7번 low-C/high20 top376/q-known 904 추가: 16-bit low-C union은 `35840/65536 = 54.6875%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top376 source 기준 모두 UNSAT가 되어 `376 * 2^26 = 25232932864`개 full `x6` 값을 parent 단위로 닫았다. q888 sidecar top4 Hensel `600,616,632,...,792` bits는 `sat=8`, `unknown=29`, `process_timeout=15`, `unsat=0`이라 hard no-good은 아니다. 별도 low600/high728 q904 frontier는 `q_low/q_prefix/q_known=608/296/904`까지 개선됐고 top4 Hensel `608,624,...,800` bits는 `sat=4`, `unknown=36`, `process_timeout=12`, `unsat=0`, root/factor 0이라 factor/plaintext 복구는 아니다.
- 7번 low-C/high20 top392/q-known 920 추가: 16-bit low-C union은 `36352/65536 = 55.46875%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top392 source 기준 모두 UNSAT가 되어 `392 * 2^26 = 26306674688`개 full `x6` 값을 parent 단위로 닫았다. low470/highscan q904 frontier도 guarded top4 Hensel `600,616,...,792,800` bits에서 `sat=5`, `unknown=17`, `process_timeout=34`, `unsat=0`으로 검증됐고, q904-base `608:8,720:8` q-growth는 `q_low/q_prefix/q_known=616/304/920`까지 개선됐다. q920 top4 Hensel `616,632,...,800` bits는 `sat=4`, `unknown=35`, `process_timeout=13`, `unsat=0`, root/factor 0이라 factor/plaintext 복구는 아니다.
- 7번 low-C/high20 top408/q-known 936 추가: 16-bit low-C union은 retry1 기준 `36864/65536 = 56.25%`까지 contiguous proof를 확장했고 roots/factors는 0이었다. high20 exact-carry closure는 broad top408 source 기준 모두 UNSAT가 되어 `408 * 2^26 = 27380416512`개 full `x6` 값을 parent 단위로 닫았다. q920-base `616:8=0x0`, `712:8=0x4` q-growth는 `q_low/q_prefix/q_known=624/312/936`까지 개선됐지만, q936 top2 Hensel `624,640,...,800` bits는 `sat=2`, `unknown=15`, `process_timeout=7`, `unsat=0`, root/factor 0이라 factor/plaintext 복구는 아니다.
- 8번: master key `2923be84e16cd6ae529049f1f1bbe9eb` 복구, 50,000개 pair mismatch 0개 확인
- 4번: 문제 PDF 외 대용량 로그/모델이 없어 실제 추출 검증 불가
